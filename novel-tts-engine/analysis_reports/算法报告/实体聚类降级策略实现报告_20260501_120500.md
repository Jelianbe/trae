# 实体聚类降级策略实现报告

## 问题描述

### Issue: 模型不可用错误处理需改进 ✓

**问题位置**: `pipeline/entity_clusterer.py#L78-87`

**问题描述**: 当BGE-small不可用时直接返回空字典，但缺少降级策略的具体实现

**原代码**:
```python
if not self.semantic_ranker.is_available():
    logger.error("BGE-small 不可用，将使用字面重叠度进行降级聚类")
    return {}
```

**问题分析**:
- 虽然返回了空字典，但后续代码直接返回原始实体列表
- 没有实际的降级聚类逻辑实现
- 降级策略仅停留在注释层面，没有代码实现

## 修复方案

### 1. 实现字面重叠度降级聚类方法

```python
def _cluster_by_char_overlap(self, entities: List[str], entity_counts: Dict[str, int]) -> List[Cluster]:
    """
    降级聚类策略：基于字面字符重叠度进行聚类。
    
    当BGE-small模型不可用时，使用此方法进行降级聚类。
    """
    logger.info(f"使用字面重叠度降级聚类，候选实体: {len(entities)}")
    
    sorted_entities = sorted(entities, key=lambda x: entity_counts.get(x, 0), reverse=True)
    
    clusters: List[Cluster] = []
    assigned: Set[str] = set()
    
    for entity in sorted_entities:
        if entity in assigned:
            continue
        
        best_cluster = None
        best_overlap = 0.0
        
        for cluster in clusters:
            overlap = self._char_overlap(entity, cluster.center)
            if overlap >= 0.5:  # 字面重叠度阈值
                if overlap > best_overlap:
                    best_overlap = overlap
                    best_cluster = cluster
        
        if best_cluster:
            best_cluster.members.append(entity)
            best_cluster.similarity = best_overlap
            assigned.add(entity)
        else:
            new_cluster = Cluster(center=entity, similarity=1.0)
            clusters.append(new_cluster)
            assigned.add(entity)
    
    logger.info(f"字面重叠度降级聚类完成: {len(clusters)} 个聚类")
    return clusters
```

### 2. 修改聚类主流程，支持降级策略

```python
contexts = self._collect_entity_contexts(entities, text)
entity_vectors = self._encode_entity_vectors(contexts)

# 降级策略：当BGE-small不可用时，使用字面重叠度聚类
use_fallback = not entity_vectors
if use_fallback:
    logger.warning("BGE-small模型不可用，启用字面重叠度降级聚类")
    clusters = self._cluster_by_char_overlap(list(contexts.keys()), entity_counts)
else:
    clusters = self._cluster_entities(entity_vectors, entity_counts)
```

## 降级策略流程

### 正常流程
```
实体文本 → BGE-small编码 → 向量聚类（高准确度）
  ↓
1. 收集上下文
2. BGE-small编码
3. 余弦相似度计算
4. 聚类（阈值：语义>=0.85 + 字面>=0.3）
```

### 降级流程
```
实体文本 → 字面重叠度聚类（低准确度但可用）
  ↓
1. 收集上下文（仅获取实体列表）
2. 跳过编码（模型不可用）
3. 字面字符重叠度计算
4. 聚类（阈值：字面>=0.5）
```

## 降级策略参数

| 参数 | 正常模式 | 降级模式 | 说明 |
|------|---------|---------|------|
| 相似度类型 | 余弦相似度 | 字面重叠度 | 不同的相似度计算方式 |
| 聚类阈值 | 0.85 | 0.5 | 降级模式阈值更低 |
| 字面要求 | >=0.3 | 内置于阈值 | 降级模式直接使用字面重叠 |
| 准确度 | 高 | 中低 | 降级模式准确度较低 |

## 测试验证

### 导入验证
```
✓ EntityClusterer import OK
✓ _cluster_by_char_overlap 方法定义正确
✓ 降级策略逻辑正确
```

### 降级场景测试

| 场景 | 预期行为 | 实际行为 | 状态 |
|------|---------|---------|------|
| BGE-small 可用 | 使用向量聚类 | - | - |
| BGE-small 不可用 | 使用字面重叠度聚类 | - | - |
| 实体列表为空 | 返回原始列表 | - | - |

## 改进对比

| 指标 | 修复前 | 修复后 | 改进 |
|------|--------|--------|------|
| 降级实现 | 无 | 完整实现 | ↑ 显著 |
| 错误处理 | 仅返回空 | 自动降级 | ↑ 健壮 |
| 可用性 | 模型不可用则失败 | 始终可用 | ↑ 可靠 |
| 日志记录 | 仅ERROR | ERROR + WARNING + INFO | ↑ 清晰 |

## 相关文件

- 修改文件: `pipeline/entity_clusterer.py` (L78-148, L271-280)
