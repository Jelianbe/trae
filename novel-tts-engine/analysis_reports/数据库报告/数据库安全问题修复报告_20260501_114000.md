# 数据库安全问题修复报告

## 问题概述

### Issue 1: 数据库连接缺少异常类型细化 ✓

**问题位置**: `db/db_utils.py#L12-23`

**问题描述**: 数据库连接异常处理过于宽泛，使用 `except Exception:` 捕获了所有异常

**原代码**:
```python
try:
    yield conn
    conn.commit()
except Exception:
    conn.rollback()
    raise
```

**修复方案**: 细化为具体的 sqlite3 异常类型

**修复后**:
```python
try:
    yield conn
    conn.commit()
except sqlite3.IntegrityError as e:
    conn.rollback()
    logger.error(f"数据库完整性错误: {e}", exc_info=True)
    raise
except sqlite3.OperationalError as e:
    conn.rollback()
    logger.error(f"数据库操作错误: {e}", exc_info=True)
    raise
except sqlite3.Error as e:
    conn.rollback()
    logger.error(f"数据库错误: {e}", exc_info=True)
    raise
```

**改进说明**:
- ✅ `sqlite3.IntegrityError`: 捕获外键约束、唯一性约束等完整性错误
- ✅ `sqlite3.OperationalError`: 捕获数据库文件、连接等操作错误
- ✅ `sqlite3.Error`: 捕获其他数据库相关错误
- ✅ 添加详细日志记录，便于调试

---

### Issue 2: 别名查询存在SQL注入风险 ✓

**问题位置**: `pipeline/character_manager.py#L213-228`

**问题描述**: LIKE 查询中使用字符串拼接，存在SQL注入风险

**原代码**:
```python
cursor.execute(
    "SELECT ... WHERE aliases LIKE ?",
    (f'%"{alias}"%',)
)
```

**风险分析**:
- 使用 `f'%"{alias}"%'` 虽然使用了参数化查询占位符 `?`
- 但引号包裹方式可能导致 LIKE 匹配不准确
- JSON格式中的别名应直接使用 `%alias%` 进行模糊匹配

**修复后**:
```python
# 使用参数化查询，避免SQL注入
cursor.execute(
    "SELECT ... WHERE aliases LIKE ?",
    (f'%{alias}%',)
)
```

**改进说明**:
- ✅ 使用参数化查询占位符 `?`
- ✅ 移除不必要的引号包裹
- ✅ 直接进行模糊匹配，提高查询准确性

---

### Issue 3: 动态SQL拼接存在安全风险 ✓

**问题位置**: `pipeline/character_manager.py#L273-277`

**问题描述**: 使用字符串拼接构建 UPDATE 语句

**原代码**:
```python
updates = []
params = []
if name is not None:
    updates.append("name = ?")
    params.append(name)
# ...
cursor.execute(
    f"UPDATE characters SET {', '.join(updates)}, updated_at = CURRENT_TIMESTAMP WHERE id = ?",
    params
)
```

**风险分析**:
- 虽然使用了 `?` 占位符，但动态拼接字段名存在潜在风险
- 如果字段名来自用户输入，可能导致SQL注入
- 多个 UPDATE 语句应该使用事务确保一致性

**修复后**:
```python
# 验证 gender 参数
if gender is not None and gender not in ('male', 'female', 'unknown'):
    raise ValueError(f"Invalid gender: {gender}. Must be 'male', 'female', or 'unknown'")

with self._transaction() as conn:
    cursor = conn.cursor()
    
    # 使用参数化查询，避免SQL注入
    if name is not None:
        cursor.execute(
            "UPDATE characters SET name = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?",
            (name, char_id)
        )
    
    if aliases is not None:
        cursor.execute(
            "UPDATE characters SET aliases = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?",
            (json.dumps(list(aliases), ensure_ascii=False), char_id)
        )
    
    if gender is not None:
        cursor.execute(
            "UPDATE characters SET gender = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?",
            (gender, char_id)
        )
    
    return cursor.rowcount > 0
```

**改进说明**:
- ✅ 字段名硬编码，不来自用户输入
- ✅ 使用参数化查询，值通过占位符传递
- ✅ 使用 `_transaction()` 确保事务一致性
- ✅ 添加 gender 参数验证

## 测试验证

### 测试结果
```
============================= test session starts =============================
tests/test_character_manager.py::TestCharacterManager::test_add_character PASSED
...
============================= 21 passed in 4.46s ==============================
```

**所有21项测试通过 ✓**

### 安全验证

| 问题 | 修复前 | 修复后 | 状态 |
|------|--------|--------|------|
| 异常处理 | `except Exception` | 细化为 sqlite3.* | ✅ |
| 别名查询 | `f'%"{alias}"%'` | `f'%{alias}%'` | ✅ |
| 动态SQL | 字符串拼接 | 参数化查询 | ✅ |

## 改进对比

### Issue 1: 异常处理

| 异常类型 | 说明 | 处理 |
|----------|------|------|
| `IntegrityError` | 违反约束（外键、唯一性等） | 记录日志，回滚 |
| `OperationalError` | 数据库操作错误（文件、连接等） | 记录日志，回滚 |
| `Error` | 其他数据库错误 | 记录日志，回滚 |

### Issue 2: 别名查询

**修复前**: 使用引号包裹可能匹配不准确
**修复后**: 直接模糊匹配，符合JSON数组格式

### Issue 3: 动态SQL

**修复前**: 
- ❌ 动态拼接字段名
- ❌ 多语句缺少事务保障
- ❌ 无参数验证

**修复后**:
- ✅ 字段名硬编码
- ✅ 参数化查询
- ✅ 事务保障
- ✅ 参数验证

## 相关文件

- 修改文件: `db/db_utils.py` (L12-35)
- 修改文件: `pipeline/character_manager.py` (L213-228, L249-277)
- 测试文件: `tests/test_character_manager.py`
