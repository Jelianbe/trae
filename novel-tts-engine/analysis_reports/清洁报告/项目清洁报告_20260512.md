# 项目清洁报告

**日期**: 2026-05-12
**执行者**: AI Agent
**备份文件**: `novel_tts.db.clean_backup`

---

## 一、执行摘要

本次清洁覆盖 4 个维度：数据库、代码修复、冗余清理、基准重建。所有任务已完成，项目状态恢复干净。

---

## 二、执行详情

### 2.1 数据库清洁（P0）

| 项目 | 清洁前 | 清洁后 |
|------|-------|-------|
| 角色总数 | 139（82 个空 project_id 污染） | 0 |
| 句子数 | 有历史残留 | 0 |
| 章节数 | 有历史残留 | 0 |
| 自增计数器 | characters: 142 | characters: 0 |

**操作**:
- 备份原数据库为 `novel_tts.db.clean_backup`
- 清空 `characters`, `characters_old`, `sentences`, `chapters`, `progress` 表
- 重置自增序列

**验证**: 测试后角色 ID 从 303/304/305 变为 1/2/3，确认从干净状态开始

---

### 2.2 代码修复（P1）

**修复项**: [pipeline_runner.py#L111](file:///d:/trae/novel-tts-engine/pipeline/pipeline_runner.py#L111) — `semantic_ranker` 未注入到 `SpeakerMatcher`

**修改前**:
```python
self.speaker_matcher = SpeakerMatcher(self.char_manager)
self.semantic_ranker = get_semantic_ranker()
```

**修改后**:
```python
self.semantic_ranker = get_semantic_ranker()
self.speaker_matcher = SpeakerMatcher(self.char_manager, semantic_ranker=self.semantic_ranker)
```

**影响**: 确保 PipelineRunner 使用统一的 semantic_ranker 实例，避免 SpeakerMatcher 创建独立单例导致的首次分析时 L2 未就绪问题

---

### 2.3 代码审计结果

| 检查项 | 状态 | 说明 |
|-------|------|------|
| linked_entities 传递 | ✅ 正确 | 仅用于句子标注，不传递到 SpeakerMatcher 是设计选择 |
| EmotionTagger 残留 | ⚠️ 保留 | `_emotion_tagger_legacy.py` 以下划线前缀标识为遗留文件，代码已迁移到 `emotion_extractor.py` |
| GUIDE_PHRASE_PATTERN | ✅ 正确 | 正则能匹配"冷笑道"等引导词，special_laughter 检查正确返回 anger |
| 单例线程锁 | ✅ 一致 | 所有 9 个单例模块均使用 `threading.Lock()` + 双重检查锁模式 |
| 双系统并行残留 | ✅ 无 | EmotionTagger 仅存遗留文件，无运行时引用 |

---

### 2.4 冗余清理

| 清理项 | 状态 |
|-------|------|
| `__pycache__/` 目录 | ✅ 已清理 |
| `.pyc` 编译文件 | ✅ 已清理 |
| `backups/` 目录 | ✅ 保留（含 `db_v3_backup.db` 为有效备份） |
| `strategic_reserve/` 目录 | ✅ 保留（储备模块，不干扰主流程） |
| `analysis_reports/` 目录 | ✅ 保留（文档和测试数据） |

---

### 2.5 前端检查

| 文件 | 状态 |
|------|------|
| `frontend/js/app.js` (1180 行) | ✅ 无语法错误 |
| `frontend/js/api.js` (424 行) | ✅ 无语法错误 |
| `frontend/index.html` | ✅ 正常引用 |

**结论**: 前端 JS 文件无语法错误，之前报告的"页面不加载"问题可能是后端服务未启动或数据库状态不一致导致，非前端代码问题。

---

## 三、基准测试结果

**测试环境**: 
- 后端: pipeline_ready=True, tts_engine=indextts
- 数据库: 空库重启后重建
- 代码: 含 semantic_ranker 修复

### 测试1: Fragments 拆分 ✅ PASS
- 6 个句子全部正确拆分为 dialogue + narration
- speaker 字段正确映射到角色名

### 测试2: 情绪数据同步 ✅ PASS
- 8 维情绪向量正确返回
- emotion_class 默认为 neutral
- TTS 生成接口正确接收 emotion 参数

### 测试3: 角色列表 ✅ PASS
- 角色 ID 从 1 开始递增（之前为 303/304/305）
- 角色数量 4（旁白 + 2 个测试角色 + 未知）

---

## 四、遗留问题（不在本次清洁范围内）

| 问题 | 优先级 | 建议处理方式 |
|------|:------:|-------------|
| EmotionTagger 遗留文件物理删除 | P2 | 确认无引用后可删除 |
| emotion_gt_100.json 数据量不足 | P2 | 补充更多人工标注样本 |
| 角色动态色差分配 | P2 | 未来功能，已规划验收清单 |
| ONNX 后端未启用（缺 optimum） | P2 | pip install optimum 后可加速 L2 加载 |
| 情绪标注基线 63% | P1 | 需优化情绪模型，非清洁问题 |

---

## 五、清洁前后对比

| 指标 | 清洁前 | 清洁后 |
|------|-------|-------|
| 数据库角色数 | 139（82 污染） | 0（干净） |
| 角色 ID 起始值 | 303 | 1 |
| semantic_ranker 注入 | ❌ 独立创建 | ✅ 统一实例 |
| __pycache__ | 存在 | 已清理 |
| 前端阻塞 | 报告有错误 | 实际无语法问题 |
| 基线可重复性 | 无法对比 | 可重复测试 |

---

## 六、下一步建议

1. **运行角色识别率基线测试**: 使用 clean DB + 修复后的代码，重新跑 v6 测试集，确认 76.4% 是否恢复
2. **重启后端服务**: 确保 production 环境使用修复后的代码
3. **建立 clean-room 测试流程**: 每次测试前自动重置数据库，防止数据污染
