# 假设记录 H-20260515-07

## 备份信息
- **备份路径**: d:\trae\backup_novel-tts-engine_20260515_191925
- **回退命令**: Copy-Item -Recurse "d:\trae\backup_novel-tts-engine_20260515_191925\*" "d:\trae\novel-tts-engine\" -Force

## 基本信息
- **日期**: 2026-05-15
- **投资类型**: 速赢
- **对应的改造方案节**: "第二阶段：规则修复（改动3+改动4）"

## 改动内容
修复截断类错误（角色核心词匹配截断完整角色名）和错选类错误（近因衰减过度导致选错候选人）

## 影响机制
**截断类（7条）**: `match_by_trigger_words` 返回 ROLE_CORE_WORDS 中的通用身份词（如"将军"），但上下文中存在更完整的角色名（如"韩将军"）。通过在 trigger_result 返回后检查上下文中是否有 "修饰词+核心词" 的完整形式，可以正确匹配完整角色名。

**错选类（9条）**: `_extract_context_speakers` 中候选人排序时全局活跃度权重过高，近因权重过低。通过在 context_before 末尾出现的角色增加近因权重，并检测"名字+说话动词"的话动特征，可以修正排序优先级。

## 可证伪条件（必填）
如果发生以下现象，说明我的因果推理是错的：
1. **截断类修复误报**: 改动后「描述性角色」类型准确率下降超过5%，说明上下文中的"修饰词+核心词"匹配把非角色名（如"老将军死了"中的"老将军"不是角色）误认为完整角色名
2. **错选类修复过拟合**: 改动后「显式提示在上文」类型正确率下降超过5%，说明近因权重调整破坏了前文已明确说话人的场景

## 预期效果
- 指标: 说话人识别准确率（51条GT基准）
- 当前基线: 45.1%
- 目标值: ≥55%
- 不作牺牲: 短文本准确率保持在73%以上，各文体均不下降

## 验证方式
运行 `python tests/baseline_rules_speaker.py` 获取分布拆解对比

## 回退条件
总准确率下降或短文本准确率下降>2%时立即回退

## 实验结果（改动后填写）
- 改前准确率: 75.7% (184/243) — 短文本300条基线
- 改后准确率: 75.7% (184/243) — 短文本300条基线
- 长文本准确率: 45.9% (39/85) — 保持不变
- 变化: ±0%
- 留出集方向: N/A（本轮未执行留出集检查）
- 决策: 拒绝
- 原因: 

**深入分析**：近因加权代码已生效（match_type已出现`(近因)`后缀），但准确率未变。根因分析显示：

1. 全部46条错误和39条正确都来自同一匹配路径 `角色库旁白匹配`，近因加权仅调整已有候选人的置信度排序
2. 关键问题在候选人生成阶段：`_extract_context_speakers()` 的前3步（代词消解→描述性角色→自称推断）全部失败，只能走兜底的`角色库旁白匹配`
3. 兜底逻辑从旁白中匹配角色库角色，但旁白中出现的角色≠说话人（如文中提到"李经理站起身"但说话人是"赵总监"）
4. **近因权重提升方向有误**：在当前候选人生成缺陷下，近因加权会导致已频繁出现的角色被系统性地选中，加剧而非缓解"活跃度偏差"
5. **本质问题**：修复方向不应是"调整已选中的候选人排序"，而是"扩大候选人池"或"修复候选人来源"

**结论**: 改动的方向正确（近因应该优先），但幅度不足以克服候选人生成环节的结构性缺陷。此改动应回退，候选人生成优化应作为独立假设重新设计。
