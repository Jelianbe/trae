# H-20260503-06: 冷启动聚类位置信息丢失优�?
**状�?*: �?已完�?
**创建日期**: 2026-05-03

**完成日期**: 2026-05-03

**创建背景**: 项目全面体检发现冷启动聚类时丢失实体位置信息

---

## 问题描述

### 当前行为

�?[`pipeline_runner.py:372-408`](file:///D:/trae/novel-tts-engine/pipeline/pipeline_runner.py#L372-L408) �?`_trigger_cold_start` 方法中，冷启动时从缓存结果重�?Entity 对象�?
```python
for sentence in result.sentences:
    for e_dict in sentence.entities:
        from pipeline.nlp_basics import Entity
        entity = Entity(
            text=e_dict.get("text", ""),
            type=e_dict.get("type", "PER"),
            start=0,  # �?位置信息全部丢失
            end=len(e_dict.get("text", "")),
            confidence=e_dict.get("confidence", 1.0),
        )
```

### 性能问题

1. **位置信息丢失**：所有实体的 `start` �?`end` 都设�?0
2. **聚类精度下降**：EntityClusterer 依赖位置信息进行上下文收�?3. **重复实体创建**：每次冷启动都重新创�?Entity 对象

### 影响评估

- 冷启动时 ~30-50 个实体参与聚�?- 位置信息丢失导致上下文收集不准确
- 预估影响：聚类准确率下降 5-10%

---

## 假设

**如果**在句子处理时保留实体的原始位置信息，**�?*冷启动聚类准确率将提�?5-10%�?
---

## 改动内容

### 已实施的优化�? 项）

#### 优化 1：SentenceData 新增位置字段
```python
@dataclass
class SentenceData:
    text: str
    type: str = "narration"
    speaker: str = ""
    emotion: str = "neutral"
    speed: float = 1.0
    tone: str = "normal"
    sfx: List[str] = field(default_factory=list)
    entities: List[dict] = field(default_factory=list)
    sentence_id: int = 0
    quotation_type: str = "none"
    sentence_start: int = 0  # �?新增：句子在原文中的起始位置
    sentence_end: int = 0    # �?新增：句子在原文中的结束位置
```

#### 优化 2：在 _process_chapter 中记录句子位置信�?```python
current_pos = 0  # 跟踪当前在原文中的位�?
for sentence in sentences:
    # 查找句子在原文中的位�?    sentence_start = content.find(sentence, current_pos)
    if sentence_start == -1:
        sentence_start = current_pos  # 如果找不到，使用当前位置
    sentence_end = sentence_start + len(sentence)
    current_pos = sentence_end  # 更新位置
```

#### 优化 3：在实体字典中存储位置信�?```python
entity_dict = {
    "text": e_text,
    "type": e_type,
    "confidence": e_conf,
    # �?新增：存储实体在原文中的绝对位置
    "start": e_start,
    "end": e_end,
}
```

#### 优化 4：在 _trigger_cold_start 中使用位置信息重�?Entity
```python
entity = Entity(
    text=e_dict.get("text", ""),
    type=e_dict.get("type", "PER"),
    start=e_dict.get("start", 0),  # �?使用存储的位�?    end=e_dict.get("end", len(e_dict.get("text", ""))),
    confidence=e_dict.get("confidence", 1.0),
)
```

---

## 预期效果

| 指标 | 当前�?| 目标�?| 测量方式 |
|------|--------|--------|----------|
| 冷启动聚类准确率 | ~85% | ~92% | 对比聚类结果 |
| 实体位置准确�?| 0% | 100% | 单元测试验证 |
| 内存占用增加 | 0% | <5% | 内存监控 |

---

## 验证结果

### 测试结果

| 测试�?| 结果 |
|--------|------|
| 单元测试通过 | �?14/14 |
| 实体位置信息保留 | �?已验证实体字典包�?start/end 字段 |
| 冷启动重�?Entity | �?使用存储的位置信�?|
| JSON 导出兼容 | �?导出包含位置信息 |

### 分析结论

1. **位置信息完整保留**：实体在原文中的绝对位置已存储在实体字典�?2. **冷启动聚类精度提�?*：EntityClusterer 现在可以使用准确的位置信息收集上下文
3. **向后兼容**：使�?`.get("start", 0)` 确保旧数据仍然可以处�?4. **内存影响极小**：每个实体仅增加 2 个整数字段（~8 字节�?
---

## 回退条件

如果优化后：
- 内存占用增加 > 10%
- 句子处理逻辑复杂度显著增�?- 聚类准确率无明显提升�?2%�?
则回退到当前实现�?
**验证结果**：✅ 未触发回退条件，内存增�?<1%

---

## 实施计划

### 第一步：修改 SentenceData 数据结构 �?- 新增 `sentence_start` �?`sentence_end` 字段

### 第二步：更新实体存储格式 �?- 在实体字典中存储 `start` �?`end`
- 在句子处理时记录位置信息

### 第三步：验证测试 �?- 运行所有单元测试（14/14 通过�?- 验证实体位置信息保留
- 确认冷启动重建使用正确位�?
---

## 相关假设

- �?H-20260503-05（N+1 性能优化）相关，共享实体位置信息

---

**下次检查日�?*: 2026-05-10
