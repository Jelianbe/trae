# 假设记录 #20260503-04

**假设编号**: H-20260503-04
**提出日期**: 2026-05-03
**状�?*: �?已完�?
---

## 改动内容

实现 `QuotationClassifier` 引号内容分类器，区分对话、书面内容、内心独白等不同类型的引号文本�?
## 影响机制

**背景**�?- 当前系统将引号内的文本一律视为对�?- 但石碑刻字、书信内容、内心独白等并非角色声带发出的声�?- TTS 引擎需要用不同的方式处理这些文本类�?
**因果推理**�?1. �?`pipeline/` 目录下新�?`quotation_classifier.py`
2. 提取所有引号内文本，通过多维度特征判断类�?3. 分类�?`WRITTEN` �?`THOUGHT` 的文本打上特殊标�?4. TTS 引擎根据标签使用不同的声音模型或处理方式

## 预期效果

- **指标**: 引号内容分类准确�?- **当前基线**: 0%（未实现分类，全部视为对话）
- **目标�?*: �?0% 分类准确�?- **其他约束**: 不影响现有说话人识别模块的稳定�?
## 四类引号场景

| 类别 | 典型例子 | TTS处理方式 |
|------|----------|-------------|
| **对话** | 萧炎道："我会回来的�? | 角色配音 |
| **书面内容** | 石碑上刻着"斗之力，三段" | 旁白/念读音效 |
| **内心独白** | 他心想："我肯定完蛋了�? | 混响/小声模型 |
| **回忆/转述** | 他说�?昨天他说'没问�?" | 整段由A配音或手动干�?|

## 验证方式

运行分类器测试脚本，验证�?1. 石碑内容正确分类�?`WRITTEN` �?2. 内心独白正确分类�?`THOUGHT` �?3. 真实对话正确分类�?`DIALOGUE` �?4. 不影响现�?NER F1 指标 �?
## 回退条件

如果分类器导�?NER F1 下降�?分，或引入新的误分类，回退到当前行为（全部视为对话）�?
## 实施计划

### 第一步：实现 `QuotationClassifier` �?�?- �?`pipeline/` 目录下新�?`quotation_classifier.py`
- 实现 `classify` 方法，返�?`DIALOGUE`, `WRITTEN`, `THOUGHT`, `UNKNOWN`
- 采用综合评分公式：score = keyword_length * 10 - distance

### 第二步：�?`PipelineRunner` 中集�?�?- �?`_process_chapter` 中调用分类器
- �?`WRITTEN` �?`THOUGHT` 打标�?- `SentenceData` 新增 `quotation_type` 字段
- 特殊类型自动设置 `speaker="Narrator"`

### 第三步：对齐 TTS 输出 �?- JSON 导出新增 `quotation_type` 字段
- TTS 引擎可根�?`quotation_type` 选择不同声音模型

## 实验结果

### 单元测试结果
- QuotationClassifier: 10/10 通过
- 管道集成测试: 28/28 通过（含管道一致性测试）
- NER F1: 保持稳定�?00.0），未受影响

### 分类准确�?- 对话场景: 100%（测试用例）
- 书面内容: 100%（测试用例）
- 内心独白: 100%（测试用例）
- 批量分类: 正确识别多个引号类型

### 决策

�?采纳并保�?
### 原因

1. 分类器采用多维度特征分析（关键词、动词、上下文窗口），不依赖硬编码规则
2. 综合评分公式（score = keyword_length * 10 - distance）兼顾特异性和距离
3. 不影响现有NER和说话人识别模块的稳定�?4. 为TTS引擎提供了区分不同引号类型的能力

---

## 补充说明

这是一个典型的�?能用"�?0分的常规场景）到"好用"�?5分的边界场景）的工程优化�?
参考方案：
- 青岛科技大学 NUCS 方法（基于叙事单元的上下文选择�?- 清华大学指代消解模型（《白鹿原》）
- 四子任务分解法（引文识别、人物识别、指代消解、说话人归属�?