# 假设记录 H-20260516-01

## 备份信息
- **备份路径**: d:\trae\backup_novel-tts-engine_20260516_130714
- **回退命令**: Copy-Item -Recurse "d:\trae\backup_novel-tts-engine_20260516_130714\*" "d:\trae\novel-tts-engine\" -Force

## 基本信息
- **日期**: 2026-05-16
- **投资类型**: 战略投资
- **任务类型**: 结构性改造
- **对应的优化项**: 工作流通用化（将算法专有流程解耦为通用骨架 + 优化项表单）

## 改动内容
将 R-021 引用的工作流文档从"说话人识别算法专用"升级为"通用任务骨架 + 按优化项类型填表"的架构。具体：抽取第0/4/6步为通用步骤，第1/2/3/5步改为根据优化项类型（算法迭代/配置调优/Bug修复/模块集成/架构重构）加载对应的表单和约束。

## 影响机制
当前工作流7个步骤中4个绑死了算法迭代场景，导致配置调优、Bug修复等其他任务被迫套用不合适的模板。解耦后，每种任务类型有独立表单，入口判断更精确，合规检查更匹配，不会出现"修复Bug还要填可证伪条件"的错配。

## 可证伪条件
如果改动后 R-021 的入口检查项超过5项（当前5项），或者新增的优化项类型表单引入比当前更多的步骤判断复杂度，说明我的"解耦降低复杂度"推理是错的。

## 预期效果
- 指标: 工作流执行步骤数/任务复杂度
- 当前基线: 1种任务类型（算法迭代），9步流程
- 目标值: 5种任务类型，通用骨架3步 + 类型特定表单 ≤ 5步
- 不作牺牲: v7.0合规检查不丢失，基线追踪不丢失

## 验证方式
1. 检查每种优化项类型的执行路径是否 ≤ 5 步（不含第-1步和第0步）
2. 用 Bug 修复类型走一遍路径，确认不会出现"可证伪条件"等不相关字段
3. 用算法迭代类型走一遍路径，确认 v7.0 合规检查仍然存在

## 回退条件
如果某种优化项类型走完后发现自己缺失了必要的检查或字段，回退并重新设计。

## 实验结果（改动后填写）
- 改前步骤数: 9
- 改后步骤数: 通用骨架 7 步（-1 到 6）+ 类型表单 6~9 步
- 新增文件数: 6（5 份表单 + 1 份通用骨架 + 1 份规则 R-022）
- 删除文件数: 0（R-021 和算法迭代手册保留）
- 验证结论: 5 种类型的执行路径结构正确，字段无错配
- 决策: 接受
