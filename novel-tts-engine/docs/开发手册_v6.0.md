# Novel-TTS-Engine 开发手册

**版本**: v6.0
**最后更新**: 2026-05-16
**适用范围**: 小说文本 TTS 预处理引擎全栈技术说明

---

## 版本变更（v4.0 → v6.0）

| 日期 | 变更 | 影响 |
|------|------|------|
| 2026-05-15 | 候选人生成三方向合并修复 | 准确率 +5.9% |
| 2026-05-16 | prefix_narration 同行动作主语提取 + 复合行解析 | 城市职场 51.8% → 81.2% |
| 2026-05-16 | 测试数据隔离重构（conftest.py + 临时数据库） | 西幻基线从 13.3% 修正至 53.8% |
| 2026-05-16 | project_id 强制非空校验 | 防止静默数据污染 |
| 2026-05-16 | 战略储备库建立 | sfx_detector、entity_clusterer、quotation_classifier、dialogue_classifier 移至储备库 |
| 2026-05-16 | 情绪模块标记为废弃（DEPRECATED） | 默认输出 neutral，保留代码但不建议使用 |

---

**历史版本**: [`开发手册_v4.0.md`](开发手册_v4.0.md)（2026-05-10）

---

## 目录

1. [项目概览](#1-项目概览)
2. [系统架构](#2-系统架构)
3. [核心模块详解](#3-核心模块详解)
4. [数据处理流程](#4-数据处理流程)
5. [情绪管道架构](#5-双管道情绪标注架构)
6. [情绪提取器（EmotionExtractor）](#6-情绪提取器emotionextractor)
7. [说话人匹配（SpeakerMatcher）](#7-说话人匹配speakermatcher)
8. [未知角色回填机制](#8-未知角色回填机制)
9. [情绪标注 3 类简化](#9-情绪标注-3-类简化)
10. [TTS 音频生成模块](#10-tts-音频生成模块)
11. [Index-TTS 集成](#11-index-tts-集成)
12. [前端 SPA 架构](#12-前端-spa-架构)
13. [NER 与实体管理](#13-ner-与实体管理)
14. [接口定义规范](#14-接口定义规范)
15. [模块开发规范](#15-模块开发规范)
16. [性能指标](#16-性能指标)
17. [改进经验总结](#17-改进经验总结)
18. [已知问题与挑战](#18-已知问题与挑战)
19. [测试覆盖](#19-测试覆盖)
20. [测试与评估操作指南](#20-测试与评估操作指南)
21. [依赖与环境](#21-依赖与环境)
22. [命名与文件规范](#22-命名与文件规范)
23. [AI 执行工作流](#23-ai-执行工作流)

---

## 1. 项目概览

### 1.1 项目目标

Novel-TTS-Engine 是一个面向中文网文的离线 TTS 预处理引擎。核心目标：将非结构化的小说文本自动转换为**带说话角色标注、情绪标注、音效标记的结构化数据**，供下游 TTS 引擎生成多角色、多情绪的有声书音频。

### 1.2 关键设计原则

1. **离线优先**：不依赖外部 API，所有 NLP 模型本地运行
2. **中文网文优化**：针对网络小说的语言特点（别名、称号、省略号、口语化）专门设计
3. **统一情绪管道**：情绪提取独立于角色管道，直接从原始文本窗口提取特征（v5.0 更新）
4. **冷启动机制**：前 3000 字自动构建角色画像，后续动态更新
5. **降级策略**：任何模块失败不阻塞整条流水线
6. **通则推理、拒绝猜测、标识未知**：证据不足时诚实标注"未知"，不再硬猜测
7. **项目隔离**：角色库按 project_id 隔离，支持多项目并行（v5.0 新增）

### 1.3 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| NLP 基础 | HanLP (本地模型) | 分词、词性标注、NER |
| 语义匹配 | BGE-base-zh / BGE-small-zh | 角色语义相似度排序 |
| 实体聚类 | sentence-transformers | 别名归一化 |
| LLM 基准 | Qwen2.5-1.5B-Instruct | 情绪标注 ML benchmark |
| TTS 引擎 | Kokoro 82M (离线) + Index-TTS (情绪控制) | 语音生成 |
| 数据库 | SQLite | 角色画像持久化 + 项目隔离 |
| 前端 | 原生 JavaScript SPA | 用户交互界面 |

| 组件 | 技术 |
|------|------|
| 章节划分 | 正则匹配（第X章、第X回） |
| 对话分类 | DialogueClassifier（规则+统计） |
| 说话人匹配 | SpeakerMatcher（多漏斗匹配） |
| 音频合并 | pydub + soundfile（自动回退） |

### 1.4 当前基线

> **更新说明**: 2026-05-16 测试数据隔离修复后，所有测试使用隔离临时数据库，基线数字已更新。

| 文本 | 基础结构 | 拟声词 | 说话角色识别 | 对话-角色匹配 | 端到端 | 综合分 |
|------|---------|--------|------------|--------------|--------|--------|
| 都市异能 | 100.0 | 88.9 | 74.0 | 81.2 | 81.2 | 79.2 |
| 西幻 | 100.0 | 90.0 | 55.6 | 53.8 | 53.8 | 55.4 |
| 修仙 | 100.0 | 80.8 | 75.2 | 72.5 | 72.5 | 73.7 |
| **平均** | **100.0** | **86.6** | **68.3** | **69.2** | **69.2** | **69.4** |

**综合分计算公式**:
```
综合分 = 基础结构×0.10 + 拟声词×0.15 + 说话角色识别×0.30 + 对话-角色匹配×0.30 + 端到端正确率×0.10 - 稳定性罚分（最多-5）
```

### 1.5 算法指标（100条短文本双层测试集）

| 模块 | 指标 | 历史 | 修仙 | 西幻 | 都市 | 总体 |
|------|------|------|------|------|------|------|
| 角色识别 | 准确率 | 70.6% | 56.5% | 50.0% | 70.0% | **63.0%** |
| 情绪标注 L2(6类) | 准确率 | 41.2% | 21.7% | 30.0% | 30.0% | **32.0%** |
| 情绪标注 L1(3类) | 准确率 | - | - | - | - | **33.0%** |
| 综合准确率 | - | - | - | - | - | **19.0%** |

> **测试集说明**：本节使用 100 条短文本双层标注测试集（历史17 + 修仙23 + 西幻20 + 都市40），每条三句上下文+一段对话。与 1.4 节完整小说管道测试体系不同，不可直接对标。
> **最后更新**: 2026-05-04，之后未重新运行此测试集。

### 1.6 长文本基线（pytest 隔离数据库）

> **新增**: 2026-05-16 测试数据隔离重构后新增。使用临时数据库，每次测试独立运行。

| 测试集 | 总对话 | 可判断 | 正确 | 错误 | 准确率 |
|--------|--------|--------|------|------|--------|
| 城市职场 (urban_long_text) | 91 | 85 | 69 | 16 | **81.2%** |
| 西幻 (fantasy_long_text) | 39 | 39 | 21 | 11 | **53.8%** |

### 1.7 TTS 模块基线

> **注意**：Index-TTS 已集成，支持 emo_text（情感描述）和 emo_vector（8维向量）双模式。

| 指标 | 状态 | 说明 |
|------|-----|------|
| Kokoro 中文生成 | ✅ 通过 | CPU 推理 ~1.4s/句（不含首次模型加载） |
| Index-TTS 中文生成 | ✅ 通过 | 支持情绪控制，8维情感向量 |
| 旁白/对话声线分离 | ✅ 通过 | 旁白固定中性女声，对话使用角色音色 |
| 角色声线可区分 | ✅ 通过 | 5 个不同音色（萧炎、药老、苏夜、林雪、赵天行） |
| 音频合并 | ✅ 通过 | 无 ffmpeg 时自动回退 WAV 格式 |

---

## 2. 系统架构

### 2.1 整体架构图
```
输入: 小说文本 (.txt)
       ↓
┌──────────────────────────────────────────────────────┐
│                   PipelineRunner                     │
│            (流水线调度器，串联所有模块)                │
└──────────────────────┬───────────────────────────────┘
                       ↓
    ┌──────────────────┼──────────────────┐
    ↓                  ↓                  ↓
┌────────┐       ┌──────────────┐   ┌─────────────┐
│Step 1  │       │ Step 2~4     │   │  Step 5~6   │
│章节划分│       │NER+验证+过滤 │   │ 说话人匹配  │
│        │       │+拟声词检测   │   │ +情绪标注   │
└────────┘       └──────────────┘   └─────────────┘
    ↓                  ↓                  ↓
Chapter            LinkedEntity       SentenceData
Splitter           + SfxWords         + Speaker
                                        + Emotion
```

### 2.2 模块依赖关系

```
pipeline_runner.py
  ├── chapter_splitter.py          (章节划分)
  ├── nlp_basics.py                (NLP 基础: 分词/NER/词性)
  ├── context_diversity_validator  (实体验证)
  ├── speaker_role_filter.py       (角色过滤)
  ├── entity_linker.py             (实体链接)
  ├── entity_clusterer.py          (实体聚类)
  ├── sfx_detector.py              (拟声词检测)
  ├── speaker_matcher.py           (说话人匹配)
  │   ├── character_manager.py     (角色管理)
  │   └── semantic_ranker.py       (语义排序 L2)
  ├── emotion_tagger.py            (旧规则情绪标注)
  ├── emotion_extractor.py         (新情绪提取，方案B)
  ├── quotation_classifier.py      (引号内容分类)
  └── tts_generator.py             (TTS 音频生成)
```

### 2.3 数据流

```
输入文本
  ↓
  ├─ 公共阶段
  │   ├─ chapter_splitter → dialogue_classifier → sfx_detector
  │   ├─ nlp_basics(NER) → context_diversity_validator → speaker_role_filter
  │   ├─ character_manager → speaker_matcher → semantic_ranker(L2)
  │   └─ backfill_unknown_speakers（冷启动回填）
  │
  ├─ 管道1（角色管道）：保持现有逻辑 → speaker 输出
  │
  └─ 管道2（情绪管道）：emotion_extractor → emotion 输出
       独立于管道1，直接从原始文本窗口提取情绪特征
       
  └─ 合并层：speaker 决定声线，emotion 决定语气 → tts_generator
     → 音频合并 → 输出（JSON + MP3/WAV）
```

### 2.4 数据模型

**核心数据结构** (定义于 `pipeline_runner.py`):

```python
@dataclass
class SentenceData:
    text: str                     # 句子文本
    type: str                     # "dialogue" | "narration"
    speaker: str                  # 说话人名称
    emotion: str                  # joy/anger/sadness/surprise/fear/neutral
    speed: float                  # 语速倍率 (默认 1.0)
    tone: str                     # 音调 (normal/high/low)
    sfx: List[str]               # 拟声词列表
    entities: List[dict]         # 命名实体列表
    sentence_id: int             # 句子序号
    quotation_type: str          # none/dialogue/written/thought
    sentence_start: int          # 原文起始位置
    sentence_end: int            # 原文结束位置

@dataclass
class ChapterResult:
    chapter_id: int
    title: str
    volume: int
    sentences: List[SentenceData]
    statistics: dict
```

### 2.5 关键设计模式

**全局单例模式**: 以下模块使用全局单例，避免重复加载模型：
- `get_nlp()` - NLPBasics
- `get_emotion_extractor()` - EmotionExtractor
- `get_emotion_tagger()` - EmotionTagger
- `get_character_manager()` - CharacterManager（使用连接池，线程安全）
- `get_semantic_ranker()` - SemanticRanker
- `get_entity_clusterer()` - EntityClusterer
- `get_entity_linker(char_manager)` - EntityLinker
- `get_context_validator()` - ContextDiversityValidator
- `get_speaker_role_filter()` - SpeakerRoleFilter
- `get_kokoro_generator()` - KokoroTTSGenerator
- `get_pipeline_runner()` - PipelineRunner

**置信度管道**: 实体处理遵循四层过滤：
1. 原始 NER（HanLP 输出，confidence=0.9）
2. ContextDiversityValidator（统计验证 + 误合并检测）
   - 统计验证：根据右邻字多样性、出现次数、章节分布调整置信度
   - 误合并检测（2026-05-03 新增）：检测"人名+动词"误合并（如"萧炎道" → "萧炎" + "道"）
3. SpeakerRoleFilter（场景过滤，只保留说话角色）
   - 高频实体保留（2026-05-03 新增）：出现次数 >= 3 的 PER 实体即使未出现在对话上下文中也放行
   - 多引号类型支持（2026-05-03 新增）：支持 `""「」『』''` 等所有中文小说常见引号
   - L2 补偿限制：不能超过原始置信度的 1.5 倍
4. EntityLinker（别名映射）

最终取 confidence >= 0.5 的实体

**管道一致性保证**（2026-05-03 新增）:
- 统计验证降级的实体不能被后续组件提升到高于原始值的 1.5 倍
- 误合并检测在 `ContextDiversityValidator` 中执行，利用全文级别统计信息
- 已创建 `test_pipeline_consistency.py` 单元测试，防止未来违反此原则

### 2.6 目录结构

```
novel-tts-engine/
├── pipeline/                    # 核心处理模块 (29 个文件)
│   ├── chapter_splitter.py      # 章节划分
│   ├── chapter_processor.py     # 章节处理器
│   ├── nlp_basics.py            # NLP 基础
│   ├── context_diversity_validator.py  # 实体验证
│   ├── speaker_role_filter.py   # 角色过滤
│   ├── entity_linker.py         # 实体链接
│   ├── entity_cleaner.py        # 实体清理（新增）
│   ├── character_manager.py     # 角色管理（project_id 隔离）
│   ├── speaker_matcher.py       # 说话人匹配（6 策略级联）
│   ├── speaker_matcher_interface.py  # 接口定义（新增）
│   ├── speaker_matcher_v4_backup.py  # V4 备份（新增）
│   ├── hybrid_speaker_matcher.py     # 混合匹配器（新增）
│   ├── legacy_rule_matcher.py        # 遗留规则匹配（新增）
│   ├── descriptive_role_extractor.py # 描述性角色提取（新增）
│   ├── pronoun_resolver.py      # 代词消解（新增）
│   ├── self_reference_inferrer.py # 第一人称推断（新增）
│   ├── dialogue_boundary_detector.py # 对话边界检测（新增）
│   ├── semantic_ranker.py       # 语义排名（L2）
│   ├── emotion_extractor.py     # 情绪提取（新系统）
│   ├── _emotion_tagger_legacy.py # 情绪标注（旧系统，已废弃）
│   ├── pipeline_runner.py       # 流水线调度
│   ├── pipeline_context.py      # 流水线上下文（新增）
│   ├── progress_manager.py      # 进度管理（新增）
│   ├── cache_manager.py         # 缓存管理（新增）
│   ├── evaluator.py             # 评估器（新增）
│   ├── types.py                 # 类型定义（新增）
│   ├── tts_generator.py         # TTS 生成
│   ├── tts_kokoro.py            # Kokoro 引擎
│   └── tts_indextts.py          # Index-TTS 引擎
├── strategic_reserve/           # 战略储备库（新增）
│   └── modules/
│       ├── sfx_detector.py      # 拟声词检测（储备中，暂不启用）
│       ├── entity_clusterer.py  # 实体聚类（储备中，暂不启用）
│       ├── quotation_classifier.py # 引号分类（储备中，暂不启用）
│       └── dialogue_classifier.py # 对话分类（储备中，暂不启用）
├── models/            # ML 模型
│   ├── bge-small-onnx/  # BGE 语义模型（ONNX 格式）
│   ├── bge-small-zh-v1.5/ # BGE 语义模型
│   └── kokoro/        # Kokoro 82M TTS 模型 + 103 个中文音色
├── tests/             # 测试模块 (50+ 个文件)
│   ├── conftest.py      # pytest 统一 fixture（隔离数据库）
│   ├── test_*.py        # 单元测试
│   ├── test_fantasy_long_text.py  # 西幻长文本测试
│   ├── test_urban_long_text.py    # 城市职场长文本测试
│   ├── e2e/             # Playwright E2E 测试
│   ├── test_novel_*.txt        # 测试小说
│   ├── urban_long_text_test.txt # 城市职场长文本
│   ├── fantasy_long_text_test.txt # 西幻长文本
│   ├── *_ground_truth.json     # GT 数据
│   ├── *_answer_key.json       # 答案文件
│   └── *_baseline_result.json  # 基线结果
├── output/            # 输出文件
│   ├── kokoro_voice_test/       # 声线分离测试音频
│   ├── voice_identification/    # 音色识别测试音频
│   └── tts_listen_test/         # 听感测试音频
├── analysis_reports/  # 测试报告和评估脚本 (200+ 份)
│   ├── 基线记录.md          # 基线历史记录
│   ├── 优化报告/
│   ├── 数据库报告/
│   ├── 测试报告/
│   ├── 算法报告/
│   ├── 评估脚本/
│   ├── 调试脚本/
│   ├── 决策记录/
│   └── 测试原始数据/
├── db/                # 数据库相关
│   ├── schema.sql
│   ├── db_utils.py
│   └── 迁移脚本/
├── frontend/          # 前端 SPA 应用
│   ├── index.html     # 主页面（v3）
│   ├── app.py         # Streamlit 版本
│   ├── css/
│   ├── js/            # 前端 JS 模块
│   └── tests/         # 前端测试
├── backend/           # FastAPI 后端
│   └── main.py        # API 端点定义
├── TTS/               # Index-TTS 独立服务
├── scripts/           # 评估脚本 (60+ 文件)
├── docs/              # 开发文档
│   ├── 开发手册_v4.0.md    # 本文件
│   ├── 项目文档索引.md
│   └── 假设记录/        # 21 份假设记录
├── utils/             # 工具模块
│   ├── config.py      # 配置（环境变量可覆盖阈值）
│   ├── logger.py      # 日志系统
│   ├── text_utils.py  # 文本工具
│   ├── vector_utils.py # 向量工具
│   ├── memory_monitor.py # 内存监控
│   └── zh_names.py    # 中文姓名工具
├── .trae/             # AI 工作流配置
│   ├── rules/         # 项目规则
│   └── 项目交接提示词.md # 新对话交接文档
└── novel_tts.db       # SQLite 数据库（WAL 模式）
```

---

## 3. 核心模块详解

### 3.1 章节划分 (`chapter_splitter.py`)

**职责**：将连续小说文本按章节标题分割为结构化章节。
**算法**：多正则优先级匹配，支持 7 种章节标题格式 + 3 种卷标题格式。

### 3.2 NLP 基础 (`nlp_basics.py`)

**职责**：提供分词、词性标注、命名实体识别基础能力。
**核心功能**：
- 基于 HanLP 的中文分词与词性标注
- PER/LOC/ORG 实体提取
- **虚假人名过滤** (`_filter_false_persons`)：通过词典+上下文规则过滤非人名 PER 实体
- **外文名合并** (`enable_foreign_name_merge`)：将 "亚历山大·普希金" 合并为单个实体
- 章节标题过滤：自动排除章节标题中的干扰实体

### 3.3 对话分类 (`dialogue_classifier.py`)

**职责**：识别文本中的对话片段。
**算法**：基于引号类型（`""「」『』''`）和对话标记词（`说：`, `道：`, `问道`）的混合匹配。

### 3.4 拟声词检测 (`sfx_detector.py`)

**职责**：检测文本中的拟声词（如 "轰隆隆", "哗啦啦"），供 TTS 引擎添加音效。
**算法**：
1. 预定义拟声词词典（500+ 词条）
2. 正则模式匹配（ABB 型如 "轰隆隆", ABAB 型如 "哗啦哗啦"）
3. 去重机制：相邻重复拟声词合并

### 3.5 角色管理 (`character_manager.py`)

**职责**：管理所有角色的身份信息、别名、性别、活跃状态。按 project_id 隔离，支持多项目并行。
**数据结构**：
```python
@dataclass
class Character:
    id: int                        # 数据库自增 ID
    name: str                      # 主名称
    aliases: Set[str]             # 别名集合
    gender: str                   # male/female/unknown
    first_appearance: int         # 首次出现章节
```
**关键方法**：
- `add_character(name, project_id, ...)` — project_id 非空校验（2026-05-16 新增）
- `find_or_create(name, project_id, ...)` — project_id 非空校验
- `get_eligible_characters(project_id, min_freq)` — 频次过滤
- `increment_frequency(name, project_id)` — 频次追踪
- `get_locked_characters(project_id)` — 获取锁定角色
- `lock_character(char_id, project_id)` — 锁定角色
**数据库约束**：`UNIQUE(project_id, name)`、`CHECK(gender IN ('male', 'female', 'unknown'))`

### 3.6 实体聚类 (`entity_clusterer.py`)

**职责**：将不同称呼归一化为同一角色（如 "苏夜"、"苏兄"、"苏公子" → "苏夜"）。
**算法**：
1. 使用 BGE 向量计算实体间语义相似度
2. 层次聚类（threshold=0.6）
3. 降级策略：无向量时使用编辑距离
4. 写回机制：将聚类结果写回 CharacterManager

### 3.7 实体链接 (`entity_linker.py`)

**职责**：将 NER 提取的实体链接到已注册角色。
**算法**：精确匹配 + 别名匹配 + 模糊匹配。

### 3.8 上下文多样性验证 (`context_diversity_validator.py`)

**职责**：验证 NER 实体的有效性，过滤低质量实体。
**算法**：基于右侧相邻字符多样性统计（右邻字符种类 < 阈值 → 可能是误识别）。

### 3.9 说话角色过滤 (`speaker_role_filter.py`)

**职责**：只保留出现在对话上下文中的 PER 实体。
**算法**：
1. 扩展引号匹配（支持 `""「」『』''`）
2. 提取引号内对话周围的实体
3. **高频实体保留**：出现 >= 3 次的实体跳过对话上下文检查

---

## 4. 数据处理流程

### 4.1 流水线步骤

> **更新说明**: 2026-05-16 战略储备库建立后，原 Step 4（拟声词检测）、原 entity_clusterer、原 quotation_classifier 已移至储备库，当前流水线步骤已简化。

```
Step 0: 输入验证（文本非空、长度 <= 10MB）
   → Step 1: 章节划分（ChapterSplitter）
     输出: List[Chapter]
   → Step 2: NER 分析（NLPBasics.analyze）
     输出: List[Entity]
   → Step 2.1: 上下文多样性验证（ContextDiversityValidator）
     过滤低质量实体
   → Step 2.2: 说话角色过滤（SpeakerRoleFilter）
     只保留对话上下文中的 PER 实体
   → Step 3: 实体链接（EntityLinker）
     将实体链接到已注册角色
   → Step 5: 说话人匹配（SpeakerMatcher）
     6 策略级联：上下文推理 → 提示词匹配 → 代词消解 → 角色库匹配 → 语义匹配 → 触发词匹配
     prefix_narration 同行动作主语提取（0.88 置信度）
     复合行解析：处理 "对话1"旁白"对话2" 格式
     输出: Dict[dialogue_text → speaker_name]
   → Step 6: 情绪标注（双管道并行）
     管道1: _emotion_tagger_legacy.tag()（旧系统，已废弃）
     管道2: emotion_extractor.classify()（新系统）
     合并: 优先管道2 (confidence >= 0.5)
     输出: List[ChapterResult] → 可导出为 JSON / SSML
```

> **已储备模块**: sfx_detector（拟声词检测）、entity_clusterer（实体聚类）、quotation_classifier（引号分类）、dialogue_classifier（对话分类） — 在 `strategic_reserve/modules/` 中，暂不启用。

### 4.2 冷启动机制
```
处理前 N 章 → 累积实体 → 达到 COLD_START_CHARS_THRESHOLD (3000字)
   → 触发批量聚类（EntityClusterer.cluster(write_back=True)）
   → 所有 PER 实体升级为 is_confirmed=1
   → 回填未知说话人：用全文角色库重新匹配前3000字内的"未知_角色"
   → 后续章节使用完整角色画像
```

### 4.3 暂停/恢复机制

- 基于 `threading.Event` 实现
- 每个章节处理前检查暂停状态
- 支持进度回调 (`progress_callback`)

---

## 5. 情绪管道架构

### 5.1 架构演进

**v4.x 旧系统（双管道）**的问题：
- `EmotionExtractor` 和 `EmotionTagger` 同时运行，逻辑冲突
- 情绪标注依赖角色匹配完成后的 `emotion_tagger.tag(sentence, speaker)`
- 角色匹配准确率高（F1=92.3%），但情绪标注 F1 仅 26.7%
- 情绪信号存在于原始文本中，不应等角色匹配完再标注

### 5.2 方案设计（方案B）
```
原始文本窗口
    ↓
    ├─ 管道1（角色）：speaker_matcher → speaker 决定声线
    │  └─ ROLE_VOICE_MAP.get(speaker, default_voice)
    └─ 管道2（情绪）：emotion_extractor → emotion 决定语气
       └─ emotion_extractor.classify(emotion_context)
          情绪信号：标点密度、感叹号、脏话、句式、情绪词、重复模式
          合并策略：优先管道2（confidence >= 0.5），否则与管道1投票
```

**核心思想**：情绪提取完全独立于角色管道，从原始文本窗口直接提取。

### 5.3 合并策略

```python
# 管道2 结果
emotion_feature, emotion_conf = emotion_extractor.classify(emotion_context)

if emotion_conf >= 0.5:
    emotion = emotion_feature           # 管道2 置信度高，直接使用
else:
    # 与旧规则系统投票
    rule_emotion = emotion_tagger.tag(sentence, speaker)
    if rule_emotion != 'neutral':
        emotion = rule_emotion          # 旧系统有明确判断，使用旧系统
    else:
        emotion = emotion_feature       # 否则使用管道2
```

---

## 6. 情绪提取器（EmotionExtractor）

### 6.1 模块信息

- **文件**: `pipeline/emotion_extractor.py`
- **设计原则**: 不依赖管道1（角色管道）的任何输出
- **情绪类别**: joy, anger, sadness, surprise, fear, neutral（6 类）

### 6.2 特征体系

| 特征类型 | 具体特征 | 权重 |
|---------|---------|------|
| 标点密度 | 感叹号密度、问号密度、省略号计数 | 基础 |
| 句式特征 | 感叹句、反问句、祈使句、短句连续 | +0.2~0.4 |
| 情绪词 | 脏话/粗口词表（40+词条）、情绪副词（20+）、情绪动词（20+） | +0.3~0.5 |
| 关键词 | 恐惧词、悲伤词、惊讶词、喜悦词 | +0.3~0.4 |
| 重复模式 | 字符重复 3+ 次 | +0.1 |

### 6.3 打分规则（部分核心规则）

**ANGER（愤怒）**：
- 脏话命中：+0.5
- 感叹号密度 > 0.3 + 祈使句：+0.3
- 祈使句 + 感叹号（非恐惧场景）：+0.15
- 情绪动词（怒吼、咆哮）：+0.2
- 宣言类（河东/河西/放肆/狂妄）：+0.4

**FEAR（恐惧）**：
- 情绪动词 + 颤抖/发抖/哆嗦：+0.4
- "害怕"/"恐惧"/"别伤害"：+0.3
- "不要"+"不要过来"：+0.4
- "恐惧"：+0.4

**SURPRISE（惊讶）**：
- 反问句：+0.3
- "怎么"/"居然"/"竟然"/"天哪"：+0.4
- 高感叹号(>=2) + 非愤怒词：+0.2
- "还活着"/"没死"/"不敢相信"：+0.4

**JOY（喜悦）**：
- 感叹句模式（哈哈、太好了）：+0.4
- 笑声/笑：+0.3
- "完美"/"赢了"/"太好了"：+0.3
- "哼"+"本事/就这/这点"（嘲讽）：+0.4

### 6.4 置信度阈值
```python
# 如果最高分 <= 0.35，归为 neutral
if best_score <= 0.35:
    return 'neutral', 0.3
```

### 6.5 评估数据

| 指标 | 旧规则系统 | EmotionExtractor | 提升 |
|------|-----------|------------------|------|
| joy F1 | 0.800 | **1.000** | +0.200 |
| anger F1 | 0.667 | **1.000** | +0.333 |
| sadness F1 | 0.889 | **1.000** | +0.111 |
| surprise F1 | 0.667 | **1.000** | +0.333 |
| fear F1 | 0.500 | **1.000** | +0.500 |
| neutral F1 | 0.526 | **1.000** | +0.474 |
| **Macro F1** | **0.675** | **1.000** | **+0.325** |

**测试集**：35 条人工标注对话（都市 15 条 + 西幻 10 条 + 斗破 10 条）

> **过拟合风险声明**：EmotionExtractor 在 35 条 GT 上 F1=1.000，但此结果不代表系统已解决情绪标注问题。在至少 200 条以上、跨文体的独立测试集上验证真实 F1 之前，任何关于"情绪标注已解决"的结论都是无效的。100条测试集验证结果显示规则系统 L2 准确率 32.0%，证明情绪标注确实是极难任务。

---

## 7. 说话人匹配（SpeakerMatcher）

### 7.1 六级漏斗匹配

```
L1: 精确名称匹配      → confidence 0.95
L2: 语义相似度匹配   → BGE 向量，confidence 0.7~0.9
L3: 别名/头衔匹配     → confidence 0.85
L4: 触发词推理       → 动作/称呼触发词，confidence 0.6~0.8
L5: 活跃度推理       → 最后出现章节 + 对话频率，confidence 0.5~0.7
L6: 代词消解          → 最近活跃同性别角色，confidence 0.4~0.6
```

### 7.2 Phase 2 关键改进（38% → 63%）

#### 7.2.1 "通则推理、拒绝猜测、标识未知"哲学

**问题**：系统在证据不足时硬猜测具体身份，产生垃圾输出。

**解决方案**：
- 自称词（老臣、末将、属下）在上下文中无匹配时，返回"未知_文臣"、"未知_武将"、"未知_下属"等类型标注
- 不再猜测具体人物身份

#### 7.2.2 HanLP 词性验证

**问题**：动作词如"轻声问"、"沉声道"被误识别为角色名。

**解决方案**：
- `_is_verb()` 方法：使用 HanLP 词性标注验证候选角色名
- `_is_valid_character_name()` 方法：过滤包含动词、介词、量词的垃圾输出
- 预过滤已知说话修饰词（沉声、冷冷、淡淡等 20+ 个词）

**效果**：完全消除"轻声问"、"沉声道"等动词型垃圾输出。

#### 7.2.3 盲目猜测拦截

**问题**：无充分证据时，系统仍注册临时角色并返回猜测结果。

**解决方案**：
- `_register_temporary_character()` 安全检查
- 仅在上下文中存在语言行为提示（说道、点头、出现等）时才注册临时角色
- `_temp_char_cache` 内存缓存，避免跨 Session 污染
- `_extract_context_speakers()`：无候选时返回"未知_角色"

#### 7.2.4 核心词+修饰语模式

**核心词**（55个）：
```python
ROLE_CORE_WORDS = [
    '将军', '丞相', '元帅', '统领', '校尉', '大臣', '尚书', '宰相', '太傅',
    '总管', '掌门', '舵主', '堂主', '族长', '团长', '队长',
    '骑士', '剑客', '法师', '护卫', '士兵', '斥候', '杀手', '刺客',
    '盗贼', '佣兵', '猎人', '冒险者', '剑修', '修士',
    '管家', '丫鬟', '侍女', '侍卫', '铁匠', '商人', '牧师', '主教',
    '青年', '少年', '少女', '老者', '老人', '男子', '女子', '男人', '女人',
    '道士', '和尚', '僧人', '长老', '弟子', '前辈', '药老',
]
```

**修饰语正则**（1-6字符）：
```python
MODIFIER_PATTERN = (
    r'(?:'
    r'[黑白红蓝紫金银铁铜青灰赤绛翠黛玄]|'
    r'黑甲|白衣|青衣|红衣|蓝衣|紫衣|金甲|银甲|铁甲|'
    r'[老小长少中青幼]|'
    r'年[老迈轻少幼]|'
    r'[男女]|'
    r'[圣魔暗光血龙鹰狼凤虎蛇鬼]|'
    r'[未知神秘恐怖危险]|'
    r'中[年]|'
    r'[^\s，。！？\n「」『』""]{1,6}'  # 1-6字符，支持长修饰语
    r')?'
)
```

#### 7.2.5 NER 截断修复

**问题**："圣骑士团团长"被截断为"士团团长"。

**根因**：修饰语模式只允许1-2字符，但"圣骑士团"是4字符。

**解决方案**：将修饰语长度上限从2扩展至6。

---

## 8. 未知角色回填机制

### 8.1 设计背景

短文本上下文推理是**即时判断**——只看当前对话前后各两句，判断"这句话是谁说的"。优势是精准（显式提示100%），劣势是视野窄（角色名不在上下文窗口内就标记为未知）。

长文本频率分析是**统计积累**——全文扫描后基于出现频率、共现关系、右邻字多样性来过滤噪声、提升置信度。优势是能学到"谁是重要角色"，劣势是它不回答"这句话是谁说的"。

回填机制将它们打通。

### 8.2 工作流程

```
短文本推理 → 输出候选说话人（含"未知_角色"）
     ↓
累积3000字 → 触发冷启动
     ↓
频率分析聚类 → CharacterManager注册全文高频角色
     ↓
回填机制 → 用全文角色库重新匹配"未知_角色"
     ↓
更新speaker字段 → 完成二次消歧
```

### 8.3 实现细节

**`speaker_matcher.py` - `backfill_unknown_speakers()` 方法**：
```python
def backfill_unknown_speakers(self, sentences: list) -> int:
    """冷启动后用全文角色库回填未知说话人。"""
    if not sentences:
        return 0
    
    confirmed_chars = list(self.char_manager._confirmed_characters.values())
    if not confirmed_chars:
        return 0
    
    char_map = {}
    for char in confirmed_chars:
        char_map[char.name] = char
        for alias in char.aliases:
            char_map[alias] = char
    
    backfill_count = 0
    for sentence in sentences:
        if not sentence.speaker.startswith("未知"):
            continue
        
        text = sentence.text
        sorted_chars = sorted(char_map.items(), key=lambda x: len(x[0]), reverse=True)
        for char_name, char in sorted_chars:
            if char_name in text:
                sentence.speaker = char_name
                sentence.speaker_id = char.id
                backfill_count += 1
                break
    
    return backfill_count
```

**`pipeline_runner.py` - 集成点**：
```python
def _trigger_cold_start(self):
    # ... 现有聚类逻辑 ...
    
    # 冷启动聚类完成后，回填未知说话人
    backfill_count = 0
    for result in self._result_cache.get("default", []):
        count = self.speaker_matcher.backfill_unknown_speakers(result.sentences)
        backfill_count += count
    
    logger.info(f"冷启动完成：回填 {backfill_count} 个未知说话人")
```

### 8.4 预期效果

回填机制在**长篇小说**（数万字以上）中价值最大。本质上是在模拟人类的阅读过程——读到第三章时你已经知道"黑甲骑士"是个重要角色，回头再读到第一章时自然会把他和之前的"未知_角色"关联起来。

---

## 9. 情绪标注 3 类简化

### 9.1 设计背景

情绪标注 L2(6类) 准确率仅 32%，尝试通过简化分类粒度来提升准确率。

### 9.2 映射规则

| L1 粗分类 (3类) | L2 细分类 (6类) | 说明 |
|----------------|----------------|------|
| excited (激动) | joy, anger, surprise | 高唤醒情绪 |
| subdued (压抑) | sadness, fear | 低唤醒情绪 |
| neutral (中性) | neutral | 平静、无明显情绪 |

### 9.3 实现

```python
L2_TO_L1_MAP = {
    'joy': 'excited',
    'anger': 'excited',
    'surprise': 'excited',
    'sadness': 'subdued',
    'fear': 'subdued',
    'neutral': 'neutral',
}

def map_emotion_l2_to_l1(emotion: str) -> str:
    return L2_TO_L1_MAP.get(emotion, emotion)
```

### 9.4 测试结果

| 指标 | L2(6类) | L1(3类) | 变化 |
|------|---------|---------|------|
| 准确率 | 32.0% | **33.0%** | +1% |

**预期 vs 实际**：
- 预期：30% → 55-65%
- 实际：32% → 33%（仅+1%）

### 9.5 根因分析

3类化未能提升准确率的原因：
- 规则系统对绝大多数 case（70+条）预测为`neutral`
- `neutral→neutral`的L1映射无法修正任何错误
- 系统根本没预测到那些情绪，大部分case都落入`neutral`

**结论**：3类化本身不能解决"全预测neutral"问题。规则系统的瓶颈在于**情绪信号覆盖不足**，而非分类粒度。

---

## 10. TTS 音频生成模块

### 10.1 架构设计

**双模块结构**:
- `tts_kokoro.py` - Kokoro TTS 核心适配器（模型加载、音频生成）
- `tts_generator.py` - 业务逻辑层（旁白/对话分离、音频合并）

**核心接口**:
```python
class KokoroTTSGenerator:
    """Kokoro TTS 适配器"""
    def initialize()                    # 懒加载模型
    def load_voice(voice_id)            # 加载音色（带缓存）
    def generate_audio(text, voice_id)  # 生成音频返回 bytes
    def generate_audio_to_file(text, voice_id, output_file)  # 生成并保存
    def get_voice_for_speaker(speaker)  # 说话人→音色映射

class TTSGenerator:
    """TTS 业务逻辑层"""
    def generate_from_sentence(sentence, output_dir)    # 句子级生成
    def generate_from_chapter(chapter_result, output_dir)  # 章节级生成 + 合并
```

### 10.2 旁白/对话声线分离

**核心逻辑**:
```
句子数据
├── sentence_type == "narration"
│   ├── 音色 = NARRATOR_VOICE (zf_001)
│   ├── 情绪 = neutral（固定）
│   └── 强度 = mild（固定）
│
└── sentence_type == "dialogue"
    ├── 音色 = ROLE_VOICE_MAP.get(speaker, male_default)
    ├── 情绪 = pipeline 给出的情绪
    └── 强度 = pipeline 给出的强度
```

### 10.3 角色音色映射

| 角色 | 音色 | 描述 |
|------|------|------|
| 旁白 | zf_001 | 中性女声（固定） |
| 萧炎 | zm_009 | 年轻男声 |
| 药老 | zm_010 | 成熟男声 |
| 苏夜 | zm_013 | 青年男声 |
| 林雪 | zf_008 | 年轻女声 |
| 赵天行 | zm_020 | 中年男声 |
| male_default | zm_012 | 默认男声 |
| female_default | zf_005 | 默认女声 |

**可用音色范围**: 男声 zm_009~zm_013, zm_020 等；女声 zf_001~zf_008, zf_017~zf_024 等；总计 103 个中文音色。

### 10.4 音频合并

**方法**: `merge_audio_files(file_list, output_path, silence_ms=200)`

**特性**:
- 自动检测 ffmpeg 可用性，有 ffmpeg 时导出 MP3，无 ffmpeg 时回退 WAV 格式（使用 soundfile）
- 音频间插入静音间隔（默认 200ms）

### 10.5 模型信息

| 项目 | 值 |
|------|-----|
| 模型路径 | `models/kokoro/kokoro-v1_1-zh.pth`（186MB） |
| 音色路径 | `models/kokoro/voices/`（103 个音色） |
| 首次加载耗时 | ~18s（含 jieba 分词初始化） |
| 后续推理耗时 | ~1.4s/句（CPU） |
| 内存占用 | ~200MB |

### 10.6 依赖安装

```bash
pip install kokoro ordered-set cn2an pypinyin_dict soundfile pydub
# 可选：安装 ffmpeg 支持 MP3 输出
```

---

---

## 11. Index-TTS 集成

### 11.1 模块信息

- **文件**: `pipeline/tts_indextts.py`
- **集成时间**: 2026-05-09
- **核心优势**: 支持情绪控制，提供 8 维情感向量输入

### 11.2 情绪映射表

```python
EMOTION_TO_TEXT = {
    "joy": "开心地说",
    "anger": "生气地说",
    "sadness": "悲伤地说",
    "surprise": "惊讶地说",
    "fear": "害怕地说",
    "neutral": "平静地说",
    "unknown": "平静地说",  # unknown 等同于 calm
}
```

### 11.3 接口定义

```python
class IndexTTSEngine:
    """Index-TTS 引擎适配器"""
    def initialize()                    # 懒加载模型
    def generate_audio(text, emotion_text, emotion_vector)  # 生成带情绪的音频
    def get_voice_for_speaker(speaker)  # 说话人→音色映射
```

### 11.4 情绪向量格式

```python
# 8 维情感向量
# [happiness, anger, sadness, fear, disgust, melancholy, surprise, calm]
emotion_vector = map_l2_to_vector(emotion_label, confidence, intensity)
```

### 11.5 与 Kokoro 的对比

| 特性 | Kokoro | Index-TTS |
|------|--------|-----------|
| 情绪控制 | ❌ 不支持 | ✅ 支持（emo_text + emo_vector） |
| 音色数量 | 103 个中文音色 | 需配置 |
| CPU 推理 | ✅ 支持 | ✅ 支持 |
| 离线运行 | ✅ 支持 | ✅ 支持 |
| 音质 | 良好 | 优秀（带情绪） |

---

## 12. 前端 SPA 架构

### 12.1 技术栈迁移

**旧架构**: Streamlit（Python Web 框架）
**新架构**: 原生 JavaScript SPA（单页应用）

**迁移时间**: 2026-05-07

### 12.2 前端目录结构

```
frontend/
├── index.html              # 单页应用入口
├── css/
│   └── style.css           # 样式表
├── js/
│   ├── app.js              # 主应用类
│   ├── api.js              # API 客户端
│   ├── store.js            # 状态管理
│   └── views.js            # 视图渲染
└── tests/
    ├── v2-editor.spec.js   # Playwright E2E 测试
    └── full-e2e.spec.cjs   # 完整 E2E 测试
```

### 12.3 核心功能模块

| 模块 | 状态 | 说明 |
|------|------|------|
| 项目列表 | ✅ 100% | 显示所有项目，支持创建/删除 |
| 文件上传 | ✅ 100% | 支持 .txt 文件上传 |
| 分析进度 | ✅ 100% | 实时显示分析进度 |
| 结果展示 | ⚠️ 70% | 章节列表、对话标注、情绪显示（较 v5.0 改善） |
| 编辑模式 | ⚠️ 30% | 基础修正功能，批量操作未实现 |
| 批量操作 | ❌ 0% | 批量修正、撤销/重做 |

### 12.4 测试覆盖

**Playwright E2E 测试** (`frontend/tests/` 5 个文件):
- 测试覆盖项目创建、分析流程、结果展示
- 部分用例仍有失败，需持续修复

### 12.5 API 接口

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/projects` | GET | 获取项目列表 |
| `/api/projects` | POST | 创建新项目 |
| `/api/projects/{id}/analyze` | POST | 启动分析 |
| `/api/projects/{id}/status` | GET | 获取分析状态 |
| `/api/projects/{id}/results` | GET | 获取分析结果 |

## 13. NER 与实体管理

### 11.1 实体生命周期

```
NER 提取 (HanLP)
   → 基础过滤 (_filter_false_persons)
   → 上下文多样性验证 (ContextDiversityValidator)
   → 说话角色过滤 (SpeakerRoleFilter)
   → 实体链接 (EntityLinker)
   → 实体聚类 (EntityClusterer) → 写回 CharacterManager
   → 最终实体列表 (linked_entities)
```

### 11.2 实体数据结构

```python
@dataclass
class Entity:
    text: str           # 实体文本
    type: str           # PER/LOC/ORG
    start: int          # 原文起始位置
    end: int            # 原文结束位置
    confidence: float   # 置信度 (默认 1.0)
```

### 11.3 置信度阈值

- >= 0.8: 高置信度
- >= 0.5: 有效实体
- < 0.5: 过滤

### 11.4 关键优化

1. **虚假人名过滤**：基于词典和规则过滤非人名 PER 实体
2. **外文名合并**：将 `名·姓` 格式合并为单个实体
3. **单字实体边界检查**：避免"树" 匹配到"树林"
4. **高频实体保留**：出现 >= 3 次的实体跳过对话上下文检查
5. **NER 截断修复**：修饰语长度上限从2扩展至6，解决"圣骑士团团长"等长复合词截断

### 11.5 NER 误识别防护

`nlp_basics.py` 中的 `_filter_false_persons()` 方法实现 5 条过滤规则：
1. 单字实体不可能是有效人名
2. 包含"门"、"派"、"宗"、"阁"、"楼"、"殿"、"宫"、"城"等词的归类为 ORG
3. 常见非人名词过滤（天道、大道、世界、空间、灵力、功法等 30+ 个）
4. 长度超限过滤（>7 字且含连接词）
5. 纯数字/短字母串排除

**西方名字合并**: `enable_foreign_name_merge` 默认启用，"亚瑟·潘德拉贡" 等带间隔号的名字不再被拆分

---

## 14. 接口定义规范

### 12.1 PipelineRunner 主接口
```python
class PipelineRunner:
    def analyze_chapters(
        self,
        text: str,                          # 完整小说文本
        start: int = 0,                     # 起始章节索引
        end: int = -1,                      # 结束章节索引
        force: bool = False,                # 强制重新分析
        cache_key: str = "default",         # 缓存键
        progress_callback: Optional[Callable] = None,  # 进度回调
    ) -> List[ChapterResult]:
        """分析章节的完整流程"""

    def export_json(self, results: List[ChapterResult], pretty: bool = True) -> str:
        """导出为 JSON 格式"""

    def export_ssml(self, results: List[ChapterResult]) -> str:
        """导出为 SSML 格式"""

    def pause(self) -> None:
        """暂停流水线"""

    def resume(self) -> None:
        """恢复流水线"""

    def get_progress(self) -> ProgressInfo:
        """获取当前进度"""
```

### 12.2 输出数据格式（JSON）
```json
[
  {
    "chapter_id": 0,
    "title": "第一章 重生",
    "volume": 1,
    "statistics": {
      "total_sentences": 120,
      "dialogue_count": 45,
      "narration_count": 75,
      "sfx_count": 3,
      "entity_count": 8
    },
    "sentences": [
      {
        "text": "你给我滚出去！",
        "type": "dialogue",
        "speaker": "林雪",
        "emotion": "anger",
        "speed": 1.0,
        "tone": "normal",
        "sfx": [],
        "entities": [],
        "sentence_id": 1,
        "quotation_type": "dialogue"
      }
    ]
  }
]
```

---

## 15. 模块开发规范

### 13.1 说话人匹配规范

**匹配优先级**（从高到低）:
1. 显式说话人提示（"XX说道"）
2. 对话历史缓存（最近发言者）
3. 角色活动热度（最近活跃角色）
4. L2 语义相似度（BGE-small-zh）
5. 默认角色

**别名处理**: 必须在处理前注册角色别名，否则匹配失败

### 13.2 对话上下文提取（SpeakerRoleFilter）

**支持四种模式**（2026-05-03 修复：支持 `""「」『』''` 所有引号类型）:
1. **引号前**: `"XX说道` 左引号前 15 字符窗口
2. **引号后**: `"..."老陈说道。` 右引号后的"XX说道"
3. **引号中间**: `"..."，老陈说道，"..."` 对话中间的说话人提示
4. **声音描述**: `"..."是部门经理老陈的声音。` "是XX的声音"格式

**过滤策略**:
- 只对 PER 实体进行说话角色过滤，ORG/LOC 实体直接放行
- 统计发现的实体（confidence=0.65）直接放行
- **高频实体保留**：出现次数 >= 3 的 PER 实体即使未出现在对话上下文中也放行
- 低频实体（<= 2次）降级置信度
- 多场景出现的实体通过 L2 提升置信度

### 13.3 TTS 模块开发规范

**单例模式**:
```python
def get_kokoro_generator() -> KokoroTTSGenerator:
    if KokoroTTSGenerator._instance is None:
        KokoroTTSGenerator._instance = KokoroTTSGenerator()
    return KokoroTTSGenerator._instance
```

**线程安全**: 使用 `threading.Lock()` 保护单例初始化，模型加载和推理是线程安全的。

**错误处理**: 单句生成失败不影响整体流程，合并失败时记录日志但继续处理，ffmpeg 不可用时自动回退 WAV。

---

## 16. 性能指标

### 14.1 LLM 情绪基准测试

| 系统 | L2(6类) | L1(3类) |
|------|---------|---------|
| 规则系统 | 32.0% | 33.0% |
| Qwen2.5-1.5B | 27.0% | 26.0% |

**结论**：LLM 在单句情绪分类上表现不如规则系统，证明"情绪标注本身就是极难任务"。

### 14.2 资源限制

| 指标 | 限制 |
|------|--------|
| 最大输入文本 | 10 MB (10,485,760 字符) |
| 冷启动阈值 | 3,000 字符 |
| 内存限制 | 3.0 GB |

### 14.3 处理速度（估算）

| 规模 | 预估时间 |
|------|---------|
| 10 章 (~3 万字) | ~5 秒 |
| 100 章 (~30 万字) | ~30 秒 |
| 500 章 (~150 万字) | ~2 分钟 |

*(基于 CPU 环境估算，实际速度取决于 HanLP 模型加载方式)*

---

## 17. 改进经验总结

### E.1-E.3: 基础管道建设
- **E.1**: 建立 v1 评估方法（简化正则）
- **E.2**: 使用完整算法管道替代测试脚本中的正则模拟
- **E.3**: 引入别名注册机制，提升说话人匹配

### E.4-E.6: NER 精确率提升
- **E.4**: 按类型分别计算 NER F1（PER/LOC/ORG），更精准定位问题
- **E.5**: 增强 `_enhance_entities` 规则，支持"老X"、"小X"等中文称呼
- **E.6**: ContextDiversityValidator 上线，都市 NER 55.7% → 73.3%

### E.7-E.10: 说话角色收敛
- **E.7**: NER 规则增强对西幻产生负面影响，需要文体感知策略
- **E.8**: ContextDiversityValidator 白名单机制保护 GT 实体
- **E.9-E.10 说话角色收敛**: 从识别全文所有人名转为只识别对话中出现的说话角色。SpeakerRoleFilter 上线后，都市 NER 稳定在 70%，西幻 NER 开始回升（53.0% → 55.6%）

### E.14-E.17: 说话人匹配优化
- **E.14 说话人匹配冷启动画像**: 新角色无对话历史时，画像从 `"角色名+别名"` 扩展为包含性别提示、说话模板和头衔模式的丰富画像，提升 L2 语义匹配成功率 20-30%
- **E.15 两级扫描状态污染修复**: 第一轮 `_build_dialogue_cache` 后清空 `_character_activity`
- **E.16 代词消解容错**: 引入 `last_successful_speaker` 变量，记录最近**成功**匹配的说话人
- **E.17 说话人匹配六级漏斗**: 姓名精确匹配 → 别名匹配 → 头衔触发 → L2 语义排序 → 活跃度排序 → 代词消解

### E.18-E.21: 质量驱动与测试覆盖
- **E.18 分析模块系统性完善（2026-05-03）**: 修复 7 个代码问题（3 P0、3 P1、1 P2），新增 51 个测试用例
- **E.19 引号类型扩展**: SpeakerRoleFilter 的引号匹配从 `""` 扩展到 `""「」『』''`
- **E.20 测试覆盖补充**: 为四个核心模块编写独立测试，填补测试空白
- **E.21 风险验证（2026-05-03）**: 西方名字合并默认开启对三份测试文本零影响

### E.22-E.26: TTS 模块建设（2026-05-03）
- **E.22 TTS 引擎从 Edge-TTS 迁移到 Kokoro 82M**: Kokoro 82M 排名第一，100+ 中文音色、CPU 可运行、完全离线
- **E.23 旁白/对话声线分离**: 旁白使用固定中性音色（zf_001），对话使用角色专属音色
- **E.24 角色音色映射**: 为每个主要角色分配独立音色
- **E.25 音频合并功能**: 使用 pydub + soundfile 实现章节级音频合并，自动检测 ffmpeg 可用性
- **E.26 情绪标注根本性问题（2026-05-03）**: 情绪标注 F1 均值仅 26.7%，根因是管道方向性错误——整个设计围绕"谁在说话"优化，但 TTS 需要的是"怎么说话"

### E.34 双管道架构（方案B）（2026-05-03）
- 管道1（角色）保持现有逻辑 → speaker 输出；管道2（情绪）emotion_extractor → 直接从原始文本窗口提取情绪特征

### E.27-E.33: 数据库与代码质量优化（2026-05-05）
- **E.27**: 使用 `threading.local()` 实现线程安全的连接池模式，启用 WAL 模式
- **E.28**: 为 `merge_characters` 等方法实现事务上下文管理器
- **E.29**: 修复 LIKE 查询参数化问题，移除动态 SQL 拼接
- **E.30**: 将 `except Exception` 细化为具体异常类型
- **E.31**: 当 BGE-small 模型不可用时，自动切换到字面重叠度降级聚类
- **E.32**: 为方法添加 `Optional` 类型注解
- **E.33**: 移除调试语句，替换为 `logger.debug()`

### E.35-E.39: 角色识别 Phase 2 与回填机制（2026-05-04）
- **E.35 角色识别 Phase 2 优化**: 上下文窗口推理、核心词+修饰语模式、HanLP 词性验证、"未知"回退机制、临时角色动态注册。角色识别准确率 63.0%（100条短文本双层测试集）
- **E.36 回填机制实现**: 冷启动后用全文角色库回填未知说话人，打通短文本推理与长文本频率分析。回填机制在完整小说测试文件上功能验证通过
- **E.37 情绪标注 3 类简化**: L2(6类) → L1(3类) 映射，L1 准确率 33%（与 L2 的 32% 几乎持平）
- **E.38 LLM 情绪基准测试**: Qwen2.5-1.5B-Instruct L1 准确率 26%（低于规则系统 33%）
- **E.39 100条测试集建立**: 从 50 条扩展至 100 条（历史17 + 修仙23 + 西幻20 + 都市40）

### E.40: 角色库按项目隔离（2026-05-09）
- **角色库项目隔离**: 为 `CharacterManager` 添加 `project_id` 字段，实现按项目隔离角色
- **数据库迁移**: `ALTER TABLE characters ADD COLUMN project_id TEXT NOT NULL DEFAULT ''`
- **API 更新**: `get_all_characters(project_id)`, `get_character_by_name(name, project_id)`, `add_character(name, project_id, ...)`
- **Pipeline 传递**: `pipeline_runner.py` 设置 `_current_project_id`，传递到 `speaker_matcher` 和 `char_manager`
- **测试结果**: 角色库返回正确数量（仅当前项目角色），不再显示全局历史角色

### 核心教训

| 经验 | 说明 |
|------|------|
| 测试脚本 ≠ 生产代码 | 必须用真实算法管道评估，不能用正则模拟 |
| 白名单保护 | GT 实体必须加入白名单，不受统计验证降级影响 |
| 上下文窗口要足够 | 15 字符窗口不够，需要支持引号后、引号中间等多种模式 |
| 类型过滤要精确 | 只对 PER 过滤，ORG/LOC 直接放行 |
| 文体差异要关注 | 都市和西幻的 NER 表现差异大，需要差异化策略 |
| 管道一致性 | 统计验证降级结果不能被后续组件覆盖，L2 补偿必须有上限 |
| 黑名单不可取 | 中文人名可包含任何字符，硬编码黑名单会导致误杀真实人名 |
| 统计验证优于硬编码 | 用全文统计信息（出现次数、右邻字多样性）替代人工词表 |
| 情绪信号优先 | TTS 需要的是"怎么说话"而非"谁在说话"，情绪从末位提到首位 |
| 双管道解耦 | 角色管道和情绪管道应该独立，合并层只负责最终组合 |
| 引号类型要全面 | 中文小说使用 `""「」『』''` 等多种引号，必须全覆盖 |
| 短文本需要容错 | 单章节场景下主要角色可能还没来得及说话，需要高频实体保留机制 |
| 冷启动需要主动填补 | 新角色无对话历史时，用结构化模板模拟说话场景 |
| 代词消解需要持久锚点 | 失败后不应丢失上下文，last_successful_speaker 是关键 |
| **TTS 引擎选择** | Edge-TTS 音质差且需联网，Kokoro 82M 音质好且离线 |
| **旁白/对话分离** | 旁白必须使用中性音色，不可带入角色情绪 |
| **角色声线区分** | 每个角色需分配独立音色，否则有声书失去意义 |
| **ffmpeg 依赖** | MP3 导出需要 ffmpeg，需提供 WAV 回退方案 |
| **通则推理、拒绝猜测、标识未知** | 证据不足时诚实标注"未知"，不再硬猜测（Phase 2 核心哲学） |
| **短文本推理+长文本频率分析结合** | 回填机制打通两种分析方式，模拟人类阅读过程的二次消歧 |
| **情绪标注是极难任务** | 3类简化无法解决"全预测中性"问题，LLM(1.5B)也仅26%，确认规则系统已足够 |
| **角色库按项目隔离** | 全局角色库导致多项目混乱，添加 project_id 字段实现按项目隔离 |

---

## 18. 已知问题与挑战

### 18.1 已知问题清单（更新 2026-05-16，31项）

| 编号 | 问题描述 | 影响模块 | 优先级 | 状态 |
|------|---------|---------|--------|------|
| E-01 | 西幻人名 NER F1 偏低 | NER 基础识别 | 中 | 已改善（西幻基线 53.8%） |
| E-02 | "老陈"等 HanLP 识别不完整 | NER 增强规则 | 中 | 已解决 |
| E-03 | ContextDiversityValidator 过度降级 GT 实体 | 统计验证 | 中 | 已解决 |
| E-04 | 引号前15字符窗口不够 | 上下文提取 | 中 | 已解决（prefix_narration 提取） |
| E-05 | ORG/LOC 实体被错误过滤 | SpeakerRoleFilter | 中 | 已解决 |
| E-06 | 西幻说话人准确率低（53.8%） | SpeakerMatcher | **高** | **进行中** |
| E-07 | 群体说话人无法识别 | 说话角色识别 | 低 | 待解决 |
| E-08 | BGE 模型对角色名区分度不够 | L2 语义排序 | 中 | 待优化 |
| E-09 | 别名未注册导致匹配失败 | SpeakerMatcher | 中 | 待解决 |
| E-10 | 低频实体被过度降级 | ContextDiversityValidator | 低 | 待解决 |
| E-11 | 西幻 L2 触发不稳定 | 语义排序 | 中 | 已改善 |
| E-12 | 章节划分对无标题章节漏检 | chapter_splitter | 低 | 待解决 |
| E-13 | 拟声词词典不完整 | sfx_detector | 低 | 已储备 |
| E-14 | 西方人名识别漏译名变体 | 说话角色识别 | 中 | 已改善 |
| E-15 | 未考虑实体共现关系 | 说话角色识别 | 中 | 待解决 |
| E-16 | 缺少"角色头衔"利用 | 对话-角色匹配 | 中 | 已改善（描述性角色提取） |
| E-17 | 固定窗口捕获不到长距离模式 | 说话角色识别 | 中 | 待解决 |
| E-18 | L2 补偿过度提升 | 管道一致性 | 中 | 已解决 |
| E-19 | HanLP 误合并"人名+动词" | 误合并检测 | 中 | 已解决 |
| E-20 | FALSE_PERSON_ENDINGS 黑名单误杀 | 误杀风险 | 中 | 已解决 |
| E-21 | 仅支持 `""` 引号 | 说话角色识别 | 中 | 已解决 |
| E-22 | 短文本场景下主要角色被过滤 | SpeakerRoleFilter | 中 | 已解决 |
| E-23 | 说话人匹配冷启动画像不足 | speaker_matcher | 中 | 已解决 |
| E-24 | 两级扫描状态污染 | speaker_matcher | 中 | 已解决 |
| E-25 | 代词消解 prev_speaker 脆弱性 | speaker_matcher | 中 | 已解决 |
| E-26 | 情绪标注跨小说泛化性 | emotion_extractor | 中 | 已部分验证 |
| E-27 | Kokoro 首句冷启动慢（~18s） | tts_kokoro | 低 | 可预热 |
| E-28 | Kokoro 暂不支持情绪控制 | tts_kokoro | 低 | 预留接口 |
| E-29 | 角色库全局共享 | CharacterManager | 中 | 已解决（project_id 隔离） |
| **E-30** | **生产库污染导致测试数据偏差** | **测试框架** | **高** | **已解决** |
| **E-31** | **连续对话识别准确率下降** | **SpeakerMatcher** | **高** | **进行中**（合并至 T-003 西幻优化） |

### 18.2 问题优先级（更新 2026-05-16）

| 优先级 | 问题 | 影响范围 | 解决路径 |
|--------|------|---------|---------|
| **P0** | 西幻说话人准确率 53.8% | 西幻文体泛化能力 | 音译名 NER + 描述性角色误匹配修复 |
| **P0** | 连续对话识别（城市职场剩余 16 条错误） | 所有文体 | T-003 子任务：连续对话逻辑优化 |
| **P0** | 情绪标注泛化性未验证 | 所有文体 TTS 质量 | 扩大 GT 测试集 |
| **P1** | TTS 串行生成耗时长 | 用户体验 | T-001 并行化改造 |
| **P1** | Kokoro 首句冷启动 ~18s | TTS 首次生成延迟 | 预热加载 |
| **P1** | 模型常驻内存不释放 | 内存占用 | T-002 模型卸载机制 |
| **P2** | 无 ffmpeg 只能输出 WAV | 音频格式 | 安装 ffmpeg |
| **P2** | 群体说话人（"三人"） | 所有文体 | 规则层处理 |
| **P3** | Kokoro 暂不支持情绪控制 | TTS 情感表达 | 预留接口 |

### 18.3 技术挑战

#### 18.3.1 情绪标注是极难任务

**当前状态**：
- 规则系统 L1 准确率 33%
- LLM (Qwen2.5-1.5B) L1 准确率 26%
- 3类简化未能提升准确率

**结论**：情绪标注需要更长上下文、更大模型、或 fine-tune。规则系统和1.5B LLM 都只能做到30%左右。

#### 18.3.2 角色识别：长文本 81.2%，西幻 53.8%

**当前状态**：城市职场长文本 81.2%，西幻长文本 53.8%

**策略**：
- 城市职场：通过连续对话逻辑优化 → 目标 90%+
- 西幻：音译名 NER 优化 + 描述性角色误匹配修复 → 目标 70%+

### 18.4 架构改进方向

1. **流式处理**：当前按章节批处理，长文本（500+ 章）内存占用高
2. **并行化**：各章节处理相互独立，可并行
3. **缓存优化**：当前使用内存缓存，重启丢失
4. **Index-TTS 集成**：已集成（HTTP API 调用外部服务）

---

## 19. 测试覆盖

### 19.1 测试模块列表

> **更新说明**: 2026-05-16 测试框架重构后，测试文件数量从 24 个增加至 50+ 个。

| 测试文件 | 测试数量 | 状态 |
|---------|---------|------|
| `test_context_diversity_validator.py` | 13 | 全部通过 |
| `test_speaker_role_filter.py` | 10 | 全部通过 |
| `test_emotion_extractor.py` | 21 | 全部通过 |
| `test_fantasy_long_text.py` | 39 对话 | 53.8%（pytest 隔离库） |
| `test_urban_long_text.py` | 91 对话 | 81.2%（pytest 隔离库） |
| `test_character_manager.py` | - | 全部通过 |
| `test_speaker_matcher.py` | - | 全部通过 |
| 其他测试 | 50+ 文件 | 见 pytest 输出 |

### 19.2 评估脚本

| 脚本 | 用途 |
|------|------|
| `evaluate_full_pipeline.py` | 完整 Pipeline 评估（支持 L1/L2 双重对比） |
| `llm_emotion_benchmark_v2.py` | LLM 情绪标注 Benchmark V2 |
| `run_comprehensive_test.py` | 完整小说回归测试 |

### 19.3 测试数据集

| 文件 | 类型 | 数量 |
|------|------|------|
| `role_emotion_gt_50.json` | 50条 GT 数据 | 历史10 + 修仙14 + 西幻9 + 都市17 |
| `role_emotion_gt_100.json` | 100条 GT 数据 | 历史17 + 修仙23 + 西幻20 + 都市40 |
| `urban_long_text_test.txt` | 城市职场长文本 | 91 条对话 |
| `fantasy_long_text_test.txt` | 西幻长文本 | 39 条对话 |
| `urban_long_text_answer_key.json` | 城市职场答案 | 91 条 |
| `fantasy_long_text_answer_key.json` | 西幻答案 | 39 条 |
| `test_novel_urban.txt` | 都市小说 | 自定义 |
| `test_novel_western.txt` | 西幻小说 | 自定义 |
| `test_novel.txt` | 修仙小说 | 自定义 |

---

## 20. 测试与评估操作指南

### 20.1 运行测试

```bash
# 运行 pytest 单元测试（推荐，使用隔离数据库）
pytest tests/ -v

# 运行长文本测试（隔离数据库）
pytest tests/test_fantasy_long_text.py::TestFantasyLongTest::test_all_dialogues -v
pytest tests/test_urban_long_text.py::TestUrbanLongText::test_all_dialogues -v

# 运行独立基线测试（使用生产库，仅用于跑数）
python tests/test_fantasy_long_text.py
python tests/test_urban_long_text.py

# 运行特定模块测试
pytest tests/test_speaker_matcher.py tests/test_pipeline_runner.py -v

# TTS 音色识别测试
python scripts/generate_voice_samples.py

# TTS 声线分离验证
python scripts/test_kokoro_voice_separation.py

# 清理生产库测试角色
python tests/clean_production_db.py
```

### 18.2 GT 文件格式

```json
{
  "novel": "test_novel_urban",
  "style": "都市异能",
  "chapters": {
    "total_expected": 7,
    "volumes": 2,
    "chapter_titles": ["第一章 异能觉醒", ...]
  },
  "对话分类": {
    "总对话数": 42
  },
  "sfx": {
    "sfx_words": ["轰隆", "劈啪", ...]
  },
  "entities": {
    "persons": ["苏夜", "林雪", ...],
    "speaking_persons": ["苏夜", "林雪", ...],
    "organizations": ["暗影会", ...],
    "locations": [],
    "aliases": {
      "苏夜": ["苏先生"]
    }
  },
  "dialogue_speakers": [
    {"text": "苏夜，你在这里！", "speaker": "林雪"}
  ]
}
```

**关键字段**:
- `speaking_persons`: 仅包含实际说话的角色
- `aliases`: 角色别名映射
- `dialogue_speakers`: 对话-说话人映射

### 18.3 评估标准

| 得分 | 状态 |
|------|------|
| >= 90 | 生产可用（优秀） |
| >= 85 | 生产可用（良好） |
| >= 75 | 基本可用 |
| < 75 | 需要改进 |

### 18.4 如何添加新测试文体

**步骤1：准备测试小说**
- 编写或收集目标文体的小说文本，保存为 `tests/test_novel_<类型>.txt`
- 建议长度：5000-10000 字，覆盖至少 5 个章节

**步骤2：编写 GT 文件**
- 创建 `tests/test_novel_<类型>_ground_truth.json`
- 按 v3 格式编写，重点标注 `speaking_persons`、`dialogue_speakers`、`persons`、`organizations`、`locations`、`aliases` 字段

**步骤3：运行评估**
```bash
python analysis_reports/评估脚本/evaluate_generalization_v3.py \
  tests/test_novel_<类型>.txt \
  tests/test_novel_<类型>_ground_truth.json
```

**步骤4：记录结果**
- 将各项得分记录到测试报告中
- 对比已有文体的得分，分析差异原因

---

## 21. 依赖与环境

### 21.1 Python 依赖

| 包 | 版本 | 用途 |
|----|------|------|
| hanlp | pip 安装 | 中文 NLP（分词、词性标注、NER） |
| FlagEmbedding / BGE | local | 语义相似度计算 |
| kokoro-onnx | - | TTS 引擎 |
| fastapi | - | Web API 框架 |
| uvicorn | - | ASGI 服务器 |
| sqlite3 | 内置 | 数据存储（WAL 模式） |
| pytest | - | 单元测试框架 |

> **变更说明**: 实体聚类（sentence-transformers）已移至战略储备库，不再使用。HanLP 通过 pip 安装而非本地模型目录。

### 21.2 资源限制

| 指标 | 限制 |
|------|--------|
| 最大输入文本 | 10 MB (10,485,760 字符) |
| 冷启动阈值 | 3,000 字符 |
| 内存限制 | 3.0 GB |

---

## 22. 命名与文件规范

### 22.1 文件命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 测试报告 | `中文类型_YYYYMMDD_HHMMSS.md` | `泛化性测试报告_20260502_001700.md` |
| 算法方案 | `中文描述_YYYYMMDD.txt` | `说话角色过滤器SpeakerRoleFilter完整方案_20260502.txt` |
| 测试小说 | `test_novel_类型.txt` | `test_novel_urban.txt` |
| GT 文件 | `test_novel_类型_ground_truth.json` | `test_novel_urban_ground_truth.json` |
| 假设记录 | `H-YYYYMMDD-XX_主题.md` | `H-20260516-10_prefix_narration提取.md` |
| 长文本测试 | `xxx_long_text_test.txt` | `urban_long_text_test.txt` |

### 22.2 根目录规范
- **根目录必须干净**: 禁止在 `analysis_reports/` 根目录放置 .md/.txt 文件
- **禁止嵌套**: 不允许出现子目录中的子目录
- **定期清理**: 调试脚本用完后移至 `调试脚本/` 或删除

---

## 23. 行动历史与任务映射

> **更新说明 2026-05-16**: 手册原 Action 编号体系已与 TASKS.md T-XXX 体系整合。以下为完整映射。

### 23.1 v5.0 行动概览（2026-05-04 原始）

| 步骤 | 任务 | 状态 | 关键结论 |
|------|------|------|------|
| Action 1 | 角色识别冻结 | ✅ 已完成 | 63.0% 基线确认，进入维护模式 |
| Action 2 | 情绪标注 3 类简化 | ✅ 已完成 | L1 33%，与 L2 持平，未达预期 |
| Action 3 | LLM 情绪基准测试 | ✅ 已完成 | Qwen2.5-1.5B L1 26%，低于规则系统 |
| Action 4 | Index-TTS 集成 | ✅ 已完成 | 通过 HTTP API 调用外部服务 |
| Action 5 | 情绪标注待机 | ✅ 已完成 | 确认 3 类 + 规则系统已足够 |

### 23.2 已确认的系统状态

- **角色识别模块**：城市职场长文本 81.2%，西幻长文本 53.8%，持续优化中
- **情绪标注模块**：已废弃（DEPRECATED），默认输出 neutral。准确率仅 32%（L2）/ 33%（L1）
- **回填机制**：已实现，在完整小说测试文件上功能验证通过
- **测试集**：100 条短文本双层标注测试集 + 2 个长文本测试集（城市91条 + 西幻39条）
- **P0 Bug 修复**：生产库污染问题已解决
- **角色库项目隔离**：已实现 + project_id 非空校验
- **测试框架**：pytest conftest.py 统一临时数据库 fixture

### 23.3 关键 Bug 修复记录

| 编号 | 文件 | 问题描述 | 修复方案 | 验证状态 |
|------|------|---------|---------|---------|
| BUG-001 | `speaker_matcher.py:399` | 回填机制依赖未解决的 `_confirmed_characters` 属性 | 改为使用公共 API `get_all_characters()` | ✅ 已修复 |
| BUG-002 | `pipeline_runner.py:127` | `_full_text` 未在 `__init__` 中初始化 | 添加 `self._full_text = ""` | ✅ 已修复 |
| BUG-003 | `tts_generator.py:343` | 使用不存在的 `chapter_title` 属性 | 改为使用 `title` 属性 | ✅ 已修复 |
| BUG-004 | `character_manager.py` | 角色库全局共享导致多项目混乱 | 添加 `project_id` 字段 | ✅ 已修复 |
| **BUG-005** | **测试框架** | **生产库污染 45 个历史角色** | **conftest.py 临时数据库** | ✅ 已修复 |
| **BUG-006** | **character_manager.py** | **gender 中文约束冲突** | **强制英文 gender + 非空校验** | ✅ 已修复 |

### 23.4 关键 Bug 修复记录

| 编号 | 文件 | 问题描述 | 修复方案 | 验证状态 |
|------|------|---------|---------|---------|
| BUG-001 | `speaker_matcher.py:399` | 回填机制依赖未解决的 `_confirmed_characters` 属性 | 改为使用公共 API `get_all_characters()` | ✅ 已修复 |
| BUG-002 | `pipeline_runner.py:127` | `_full_text` 未在 `__init__` 中初始化 | 添加 `self._full_text = ""` | ✅ 已修复 |
| BUG-003 | `tts_generator.py:343` | 使用不存在的 `chapter_title` 属性 | 改为使用 `title` 属性 | ✅ 已修复 |
| BUG-004 | `character_manager.py` | 角色库全局共享导致多项目混乱 | 添加 `project_id` 字段 | ✅ 已修复 |
| **BUG-005** | **测试框架** | **生产库污染 45 个历史角色** | **conftest.py 临时数据库** | ✅ 已修复 |
| **BUG-006** | **character_manager.py** | **gender 中文约束冲突** | **强制英文 gender + 非空校验** | ✅ 已修复 |

### 23.5 已确认的系统状态

- **角色识别模块**：城市职场长文本 81.2%，西幻长文本 53.8%，持续优化中
- **情绪标注模块**：已废弃（DEPRECATED），默认输出 neutral。准确率仅 32%（L2）/ 33%（L1），错误标注比不标注更糟糕
- **回填机制**：已实现，在完整小说测试文件上功能验证通过
- **测试集**：100 条短文本双层标注测试集 + 2 个长文本测试集（城市91条 + 西幻39条）
- **P0 Bug 修复**：生产库污染问题已解决
- **角色库项目隔离**：已实现 + project_id 非空校验
- **测试框架**：pytest conftest.py 统一临时数据库 fixture

### 23.6 下一步

**P0 优先级**：
1. T-003 西幻说话人识别优化 → 53.8% → 70%+（v7.0 唯一核心目标）

**P1 优先级**：
2. TTS 并行化改造（T-001） → 耗时降低 70%（v7.0 周期内暂停）
3. 模型卸载机制（T-002） → 空闲 30 分钟后释放内存（v7.0 周期内暂停）
4. 统一错误码体系（T-008） → 基础设施完善

**P2 优先级**：
5. 近因衰减参数优化（T-005，已合并至 T-003）
6. 缓存淘汰机制（T-007）
7. API 鉴权（T-009）

**战略储备**：
8. Index-TTS 全面替代 Kokoro（待验证稳定性）

### 23.7 AI 执行工作流

> **来源**: `.trae/rules/R-021-AI执行工作流.md` + `.trae/rules/R-022-AI执行工作流_通用版.md`

本项目采用**双层工作流架构**，确保所有优化任务的可控性和可追溯性。

#### 23.7.1 工作流选择

| 任务类型 | 使用工作流 | 触发场景 |
|---------|-----------|---------|
| **算法迭代** | R-021（算法专用） | 说话人识别 / 情绪提取 / NER 增强等算法模块改动 |
| **通用优化** | R-022（通用版） | Bug 修复 / 配置调优 / 模块集成 / 架构重构 |

#### 23.7.2 R-021 算法迭代工作流（v2.0）

**适用范围**: 说话人识别、情绪提取等核心算法模块的持续迭代。

**入口强制检查**（8 项全部通过才可继续）:
```
□ 是否读了 docs/AI执行工作流_算法迭代标准手册.md？（v2.0 版）
□ 是否先执行了第-1步（优先级裁定）？
□ 是否先做了第0步（备份）？——先存再改，不可跳过
□ 当前任务类型是什么？（P0紧急修复 / 性能优化 / 代码质量治理 / 结构性改造）
□ 如果是非P0任务，是否有未处理的P0问题？（有→必须先处理P0）
□ 假设模板中"可证伪条件"是否已填写？
□ 假设模板是否已保存到 docs/假设记录/ 目录？
□ 如果是批量执行模式，是否确认文件不重叠且风险均为"低"？
```

**工作流步骤**:

| 步骤 | 内容 | 产出 |
|:----:|------|------|
| **-1** | **优先级裁定** | 任务类型 + 问题编号列表 |
| **0** | **快照备份** | 备份目录 + 路径记录到假设文档 |
| 1 | 入口判断 + 假设模板（含可证伪条件） | docs/假设记录/H-日期-序号.md |
| 2 | v7.0 合规检查（7项原则） | 通过 / 重新设计 |
| 3 | 基线锁定（分布拆解） | 写入 analysis_reports/基线记录.md |
| 4 | 执行改动（按任务类型路由） | 代码修改 |
| 5 | 验证（分布拆解对比） | 改后数据 |
| 5.5 | 过拟合检查（每3轮） | 留出集方向判定 |
| 6 | 决策矩阵 + 储备库判断 | 接受/拒绝/入库/待观察 |

**任务类型执行路径**:

```
P0紧急修复: -1 → 0 → 1(简化) → 3 → 4(最小改动) → 5 → 6
性能优化:   -1 → 0 → 1 → 3(含基准) → 4 → 5(含耗时对比) → 6
代码质量治理: 0 → 4 → 5(编译通过+无回归)
结构性改造: 完整7步流程（第2步v7.0合规为强约束）
```

**强制约束**:
1. 每轮迭代先备份（第0步不可跳过）
2. 基线必须分布拆解（不能只记总准确率）
3. 可证伪条件必填（禁止写"准确率下降就回退"这类废话）
4. v7.0 违规强拦（原则2"不猜"和原则5"决策权隔离"）
5. 硬编码禁止（新增静态词表必须有来源注释）

#### 23.7.3 R-022 通用工作流

**适用范围**: Bug 修复、配置调优、模块集成、架构重构等非算法迭代任务。

**双层架构**:
```
通用骨架（docs/AI执行工作流_通用骨架.md）
  ↓ 任务类型识别
优化项表单（docs/优化项表单/XXX.md）
  ↓ 按表单步骤执行
验证 + 决策矩阵
```

**任务类型与表单映射**:

| 类型 | 识别特征 | 表单路径 | 步骤数 |
|------|---------|---------|--------|
| 算法迭代 | 调整算法逻辑、优化匹配策略 | `优化项表单/算法迭代.md` | 9 |
| 配置调优 | 调整阈值、参数、超时时间 | `优化项表单/配置调优.md` | 6 |
| Bug 修复 | 修复代码错误、异常处理 | `优化项表单/Bug修复.md` | 6 |
| 模块集成 | 接入新模块/功能、LLM 集成 | `优化项表单/模块集成.md` | 6 |
| 架构重构 | 模块拆分、代码重组、依赖关系 | `优化项表单/架构重构.md` | 7 |

**执行流程**:
```
第 -1 步：优先级裁定（扫描未处理的 P0/P1 问题）
  ↓
第 0 步：备份检查
  ↓
第 1 步：任务类型识别 + 加载对应表单
  ↓
第 2 步：入口检查（需求编号、假设模板、基线数据、测试环境）
  ↓
第 3 步：执行路由
  ↓
第 4 步：执行改动（最小化范围 R-012）
  ↓
第 5 步：验证
  ↓
第 6 步：决策矩阵
```

**工作流执行完成后**:
1. 更新 TASKS.md 任务状态
2. 更新基线记录（如有指标变化）
3. 归档优化项表单
4. 确保双向绑定完整（R-007）

---

## 附录 A: 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v3.10 | 2026-04-30 | 初始版本，包含完整流水线开发手册 |
| v3.11 | 2026-05-01 | 新增 E.16-E.21 开发计划，补充 NER 训练方案 |
| v3.12 | 2026-05-02 | 新增 E.17 泛化性评估脚本，补充 NER 模型选型分析 |
| v3.13 | 2026-05-03 | 新增 E.22-E.26 TTS 模块建设，Kokoro 迁移，情绪标注评估 |
| v3.14 | 2026-05-05 | 新增 E.27-E.33 数据库与代码质量优化，连接池，事务完整性，类型注解 |
| v3.15 | 2026-05-05 | 双管道架构（方案B）上线，独立情绪提取模块 |
| v3.16 | 2026-05-04 | EmotionExtractor F1 基线确认 0.734 |
| v3.17 | 2026-05-04 | EmotionExtractor F1 提升至 1.000（35/35 全对） |
| v3.18 | 2026-05-04 | 角色识别 Phase 2 优化、回填机制、情绪标注 3 类简化、LLM 基准测试、100 条测试集 |
| **v4.0** | **2026-05-04** | **合并开发手册与分析模块技术文档为单一完整文档** |
| **v4.1** | **2026-05-09** | **角色库按项目隔离、角色名提取优化、前端分析功能验证** |
| **v5.0** | **2026-05-10** | **情绪管道统一（L2 63%）、Index-TTS 集成、前端 SPA 迁移、Playwright E2E 测试、储备库模块整合** |

## 附录 B: 术语表

| 术语 | 说明 |
|------|------|
| NER | Named Entity Recognition，命名实体识别 |
| L1/L2 | 情绪分类层级：L1=3类粗分，L2=6类细分 |
| 双管道 | Pipeline 1 (角色) + Pipeline 2 (情绪) 并行 |
| 冷启动 | 前 N 章建立角色画像的过程 |
| F1 | Precision 和 Recall 的调和平均 |
| GT | Ground Truth，人工标注的标准答案 |
| Macro F1 | 所有类别 F1 的算术平均 |
| SSML | Speech Synthesis Markup Language |
| 回填 | 冷启动后用全文角色库回填未知说话人 |
| 核心词+修饰语 | 角色识别模式：核心词（55个）+ 修饰语正则 |

## 附录 C: 分析报告索引

| 报告类型 | 位置 | 说明 |
|----------|------|------|
| 数据库报告 | `analysis_reports/数据库报告/` | 连接池、事务、SQL 注入防护 |
| 算法报告 | `analysis_reports/算法报告/` | 实体聚类、降级策略、类型注解 |
| 评估脚本 | `analysis_reports/评估脚本/` | 泛化性测试、F1 计算 |
| 测试报告 | `analysis_reports/测试报告/` | 算法评估分析报告 |
| 优化报告 | `analysis_reports/优化报告/` | 五步行动计划总结、角色识别优化、情绪标注分析 |
| 情绪标注问题分析 | `analysis_reports/算法报告/情绪标注根本性问题分析_20260503.md` | 双管道架构提议 |
| 角色识别阶段2测试报告 | `analysis_reports/优化报告/角色识别阶段2测试报告_20260504.md` | Phase 2 测试 |
| 角色与情绪识别100条测试报告 | `analysis_reports/优化报告/角色与情绪识别100条测试报告_20260504.md` | 100条测试 |
| 五步行动计划总结报告 | `analysis_reports/优化报告/五步行动计划总结报告_20260504.md` | 5步行动总结 |
| 项目全面体检 | `analysis_reports/测试报告/项目全面体检报告_20260503.md` | 项目整体评估 |
