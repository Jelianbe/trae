# Novel-TTS-Engine 分析模块技术文档

**版本**: v2.0  
**更新日期**: 2026-05-04  
**适用范围**: 小说文本 TTS 预处理引擎分析模块全栈技术说明

---

## v2.0 更新摘要

本次更新涵盖 2026-05-04 的重大开发成果：

### 核心改进

1. **角色识别 Phase 2 优化**（38% → 63%）
   - 上下文窗口推理：基于对话前后文的即时说话人判断
   - "未知"回退机制：证据不足时诚实标注，不再硬猜测
   - 核心词+修饰语模式：55个核心词替代穷举模式
   - HanLP 词性验证：过滤动词型垃圾输出
   - NER 截断修复：修饰语长度从2扩展至6字符

2. **回填机制**（未知角色二次消歧）
   - 冷启动后用全文角色库回填前3000字的未知说话人
   - 短文本推理 + 长文本频率分析打通
   - 模拟人类阅读过程：读到后面再回头关联前面的"未知角色"

3. **情绪标注 3 类简化**
   - L2(6类) → L1(3类) 映射：joy/anger/surprise → excited, sadness/fear → subdued
   - L1(3类) 准确率 33%（与 L2 的 32% 几乎持平）
   - 验证了"情绪标注本身就是极难任务"的判断

4. **LLM 情绪基准测试**
   - Qwen2.5-1.5B-Instruct L1 准确率 26%（低于规则系统 33%）
   - 证明角色缺失不是情绪信号压制的主因
   - 确认 3类+规则系统 已足够

5. **100条测试集建立**
   - 从50条扩展至100条（历史17 + 修仙23 + 西幻20 + 都市40）
   - 角色识别 63%，情绪标注 33%

---

## 目录

1. [项目概览](#1-项目概览)
2. [系统架构](#2-系统架构)
3. [核心模块详解](#3-核心模块详解)
4. [数据处理流程](#4-数据处理流程)
5. [双管道情绪标注架构](#5-双管道情绪标注架构)
6. [情绪提取器（EmotionExtractor）](#6-情绪提取器emotionextractor)
7. [说话人匹配（SpeakerMatcher）](#7-说话人匹配speakermatcher)
8. [未知角色回填机制](#8-未知角色回填机制) **[新增]**
9. [情绪标注 3 类简化](#9-情绪标注-3-类简化) **[新增]**
10. [NER 与实体管理](#10-ner-与实体管理)
11. [接口定义规范](#11-接口定义规范)
12. [性能指标](#12-性能指标)
13. [测试覆盖](#13-测试覆盖)
14. [当前问题与挑战](#14-当前问题与挑战)
15. [依赖与环境](#15-依赖与环境)

---

## 附录
- [附录 A: 版本历史](#附录-a-版本历史)
- [附录 B: 相关文档索引](#附录-b-相关文档索引)
- [附录 C: 术语表](#附录-c-术语表)
- [附录 D: 情绪标注验证数据](#附录-d-情绪标注验证数据gt-标注--测试脚本--结果)
- [附录 E: 五步行动计划总结](#附录-e-五步行动计划总结) **[新增]**

---

## 1. 项目概览

### 1.1 项目目标

Novel-TTS-Engine 是一个面向中文网文的离线 TTS 预处理引擎。核心目标：将非结构化的小说文本自动转换为**带说话角色标注、情绪标注、音效标记的结构化数据**，供下游 TTS 引擎（如 Kokoro）生成多角色、多情绪的有声书音频。

### 1.2 关键设计原则

1. **离线优先**：不依赖外部 API，所有 NLP 模型本地运行
2. **中文网文优化**：针对网络小说的语言特点（别名、称号、省略号、口语化）专门设计
3. **双管道架构**：角色识别与情绪标注并行处理，互不依赖
4. **冷启动机制**：前 3000 字自动构建角色画像，后续动态更新
5. **降级策略**：任何模块失败不阻塞整条流水线
6. **通则推理、拒绝猜测、标识未知**：证据不足时诚实标注"未知"，不再硬猜测

### 1.3 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| NLP 基础 | HanLP (本地模型) | 分词、词性标注、NER |
| 语义匹配 | BGE-base-zh | 角色语义相似度排序 |
| 实体聚类 | sentence-transformers | 别名归一化 |
| LLM 基准 | Qwen2.5-1.5B-Instruct | 情绪标注 ML benchmark |
| TTS 引擎 | Kokoro (离线) | 语音生成 |
| 数据库 | SQLite | 角色画像持久化 |

---

## 2. 系统架构

### 2.1 整体架构图
```
输入: 小说文本 (.txt)
       ↓
┌──────────────────────────────────────────────────────┐
│                   PipelineRunner                     │
│            (流水线调度器，串联所有模块)                │
└──────────────────────┬───────────────────────────────┘
                       ↓
    ┌──────────────────┼──────────────────┐
    ↓                  ↓                  ↓
┌────────┐       ┌──────────────┐   ┌─────────────┐
│Step 1  │       │ Step 2~4     │   │  Step 5~6   │
│章节划分│       │NER+验证+过滤 │   │ 说话人匹配  │
│        │       │+拟声词检测   │   │ +情绪标注   │
└────────┘       └──────────────┘   └─────────────┘
    ↓                  ↓                  ↓
Chapter            LinkedEntity       SentenceData
Splitter           + SfxWords         + Speaker
                                        + Emotion
```

### 2.2 模块依赖关系

```
pipeline_runner.py
  ├── chapter_splitter.py          (章节划分)
  ├── nlp_basics.py                (NLP 基础: 分词/NER/词性)
  ├── ner_extractor.py             (NER 提取接口)
  ├── context_diversity_validator  (实体验证)
  ├── speaker_role_filter.py       (角色过滤)
  ├── entity_linker.py             (实体链接)
  ├── entity_clusterer.py          (实体聚类)
  ├── sfx_detector.py              (拟声词检测)
  ├── speaker_matcher.py           (说话人匹配)
  │   ├── character_manager.py     (角色管理)
  │   └── semantic_ranker.py       (语义排序 L2)
  ├── emotion_tagger.py            (旧规则情绪标注)
  ├── emotion_extractor.py         (新情绪提取，方案B)
  ├── quotation_classifier.py      (引号内容分类)
  └── tts_generator.py             (TTS 音频生成)
```

### 2.3 数据模型

**核心数据结构** (定义于 `pipeline_runner.py`):

```python
@dataclass
class SentenceData:
    text: str                     # 句子文本
    type: str                     # "dialogue" | "narration"
    speaker: str                  # 说话人名称
    emotion: str                  # joy/anger/sadness/surprise/fear/neutral
    speed: float                  # 语速倍率 (默认 1.0)
    tone: str                     # 音调 (normal/high/low)
    sfx: List[str]               # 拟声词列表
    entities: List[dict]         # 命名实体列表
    sentence_id: int             # 句子序号
    quotation_type: str          # none/dialogue/written/thought
    sentence_start: int          # 原文起始位置
    sentence_end: int            # 原文结束位置

@dataclass
class ChapterResult:
    chapter_id: int
    title: str
    volume: int
    sentences: List[SentenceData]
    statistics: dict
```

---

## 3. 核心模块详解

### 3.1 章节划分 (`chapter_splitter.py`)

**职责**：将连续小说文本按章节标题分割为结构化章节。
**算法**：多正则优先级匹配，支持 7 种章节标题格式 + 3 种卷标题格式。

### 3.2 NLP 基础 (`nlp_basics.py`)

**职责**：提供分词、词性标注、命名实体识别基础能力。
**核心功能**：
- 基于 HanLP 的中文分词与词性标注
- PER/LOC/ORG 实体提取
- **虚假人名过滤** (`_filter_false_persons`)：通过词典+上下文规则过滤非人名 PER 实体
- **外文名合并** (`enable_foreign_name_merge`)：将 "亚历山大·普希金" 合并为单个实体
- 章节标题过滤：自动排除章节标题中的干扰实体

### 3.3 对话分类 (`dialogue_classifier.py`)

**职责**：识别文本中的对话片段。
**算法**：基于引号类型（`""「」『』''`）和对话标记词（`说：`, `道：`, `问道`）的混合匹配。

### 3.4 拟声词检测 (`sfx_detector.py`)

**职责**：检测文本中的拟声词（如 "轰隆隆", "哗啦啦"），供 TTS 引擎添加音效。
**算法**：
1. 预定义拟声词词典（500+ 词条）
2. 正则模式匹配（ABB 型如 "轰隆隆", ABAB 型如 "哗啦哗啦"）
3. 去重机制：相邻重复拟声词合并

### 3.5 角色管理 (`character_manager.py`)

**职责**：管理所有角色的身份信息、别名、性别、活跃状态。
**数据结构**：
```python
@dataclass
class Character:
    name: str                      # 主名称
    aliases: Set[str]             # 别名集合
    gender: str                   # male/female/unknown
    titles: Set[str]             # 头衔/称号
    is_confirmed: bool           # 是否已确认
    dialogue_history: List[str]  # 对话历史（用于画像更新）
    last_seen_chapter: int       # 最后出现章节
```

### 3.6 实体聚类 (`entity_clusterer.py`)

**职责**：将不同称呼归一化为同一角色（如 "苏夜"、"苏兄"、"苏公子" → "苏夜"）。
**算法**：
1. 使用 BGE 向量计算实体间语义相似度
2. 层次聚类（threshold=0.6）
3. 降级策略：无向量时使用编辑距离
4. 写回机制：将聚类结果写回 CharacterManager

### 3.7 实体链接 (`entity_linker.py`)

**职责**：将 NER 提取的实体链接到已注册角色。
**算法**：精确匹配 + 别名匹配 + 模糊匹配。

### 3.8 上下文多样性验证 (`context_diversity_validator.py`)

**职责**：验证 NER 实体的有效性，过滤低质量实体。
**算法**：基于右侧相邻字符多样性统计（右邻字符种类 < 阈值 → 可能是误识别）。

### 3.9 说话角色过滤 (`speaker_role_filter.py`)

**职责**：只保留出现在对话上下文中的 PER 实体。
**算法**：
1. 扩展引号匹配（支持 `""「」『』''`）
2. 提取引号内对话周围的实体
3. **高频实体保留**：出现 >= 3 次的实体跳过对话上下文检查

---

## 4. 数据处理流程

### 4.1 流水线步骤
```
Step 0: 输入验证（文本非空、长度 <= 10MB）
   → Step 1: 章节划分（ChapterSplitter）
     输出: List[Chapter]
   → Step 2: NER 分析（NLPBasics.analyze）
     输出: List[Entity]
   → Step 2.1: 上下文多样性验证（ContextDiversityValidator）
     过滤低质量实体
   → Step 2.2: 说话角色过滤（SpeakerRoleFilter）
     只保留对话上下文中的 PER 实体
   → Step 3: 实体链接（EntityLinker）
     将实体链接到已注册角色
   → Step 4: 拟声词检测（SfxDetector）
     输出: List[SfxWord]
   → Step 5: 说话人匹配（SpeakerMatcher）
     输出: Dict[dialogue_text → speaker_name]
   → Step 6: 情绪标注（双管道并行）
     管道1: emotion_tagger.tag()（基于规则的旧系统）
     管道2: emotion_extractor.classify()（基于文本特征的新系统）
     合并: 优先管道2 (confidence >= 0.5)
     输出: List[ChapterResult] → 可导出为 JSON / SSML
```

### 4.2 冷启动机制
```
处理前 N 章 → 累积实体 → 达到 COLD_START_CHARS_THRESHOLD (3000字)
   → 触发批量聚类（EntityClusterer.cluster(write_back=True)）
   → 所有 PER 实体升级为 is_confirmed=1
   → **回填未知说话人**（新增）：用全文角色库重新匹配前3000字内的"未知_角色"
   → 后续章节使用完整角色画像
```

### 4.3 暂停/恢复机制

- 基于 `threading.Event` 实现
- 每个章节处理前检查暂停状态
- 支持进度回调 (`progress_callback`)

---

## 5. 双管道情绪标注架构

### 5.1 架构背景

旧系统（单管道）的问题：
- 情绪标注依赖角色匹配完成后的 `emotion_tagger.tag(sentence, speaker)`
- 角色匹配准确率高（F1=92.3%），但情绪标注 F1 仅 26.7%
- 情绪信号存在于原始文本中，不应等角色匹配完再标注

### 5.2 方案设计（方案B）
```
原始文本窗口
    ↓
    ├─ 管道1（角色）：speaker_matcher → speaker 决定声线
    │  └─ ROLE_VOICE_MAP.get(speaker, default_voice)
    └─ 管道2（情绪）：emotion_extractor → emotion 决定语气
       └─ emotion_extractor.classify(emotion_context)
          情绪信号：标点密度、感叹号、脏话、句式、情绪词、重复模式
          合并策略：优先管道2（confidence >= 0.5），否则与管道1投票
```

**核心思想**：情绪提取完全独立于角色管道，从原始文本窗口直接提取。

### 5.3 合并策略

```python
# 管道2 结果
emotion_feature, emotion_conf = emotion_extractor.classify(emotion_context)

if emotion_conf >= 0.5:
    emotion = emotion_feature           # 管道2 置信度高，直接使用
else:
    # 与旧规则系统投票
    rule_emotion = emotion_tagger.tag(sentence, speaker)
    if rule_emotion != 'neutral':
        emotion = rule_emotion          # 旧系统有明确判断，使用旧系统
    else:
        emotion = emotion_feature       # 否则使用管道2
```

---

## 6. 情绪提取器（EmotionExtractor）

### 6.1 模块信息

- **文件**: `pipeline/emotion_extractor.py`
- **设计原则**: 不依赖管道1（角色管道）的任何输出
- **情绪类别**: joy, anger, sadness, surprise, fear, neutral（6 类）

### 6.2 特征体系

| 特征类型 | 具体特征 | 权重 |
|---------|---------|------|
| 标点密度 | 感叹号密度、问号密度、省略号计数 | 基础 |
| 句式特征 | 感叹句、反问句、祈使句、短句连续 | +0.2~0.4 |
| 情绪词 | 脏话/粗口词表（40+词条）、情绪副词（20+）、情绪动词（20+） | +0.3~0.5 |
| 关键词 | 恐惧词、悲伤词、惊讶词、喜悦词 | +0.3~0.4 |
| 重复模式 | 字符重复 3+ 次 | +0.1 |

### 6.3 打分规则（部分核心规则）

**ANGER（愤怒）**：
- 脏话命中：+0.5
- 感叹号密度 > 0.3 + 祈使句：+0.3
- 祈使句 + 感叹号（非恐惧场景）：+0.15
- 情绪动词（怒吼、咆哮）：+0.2
- 宣言类（河东/河西/放肆/狂妄）：+0.4

**FEAR（恐惧）**：
- 情绪动词 + 颤抖/发抖/哆嗦：+0.4
- "害怕"/"恐惧"/"别伤害"：+0.3
- "不要"+"不要过来"：+0.4
- "恐惧"：+0.4

**SURPRISE（惊讶）**：
- 反问句：+0.3
- "怎么"/"居然"/"竟然"/"天哪"：+0.4
- 高感叹号(>=2) + 非愤怒词：+0.2
- "还活着"/"没死"/"不敢相信"：+0.4

**JOY（喜悦）**：
- 感叹句模式（哈哈、太好了）：+0.4
- 笑声/笑：+0.3
- "完美"/"赢了"/"太好了"：+0.3
- "哼"+"本事/就这/这点"（嘲讽）：+0.4

### 6.4 评估数据

| 指标 | 旧规则系统 | EmotionExtractor | 提升 |
|------|-----------|------------------|------|
| joy F1 | 0.800 | **1.000** | +0.200 |
| anger F1 | 0.667 | **1.000** | +0.333 |
| sadness F1 | 0.889 | **1.000** | +0.111 |
| surprise F1 | 0.667 | **1.000** | +0.333 |
| fear F1 | 0.500 | **1.000** | +0.500 |
| neutral F1 | 0.526 | **1.000** | +0.474 |
| **Macro F1** | **0.675** | **1.000** | **+0.325** |

**测试集**：35 条人工标注对话（都市 15 条 + 西幻 10 条 + 斗破 10 条）

### 6.5 置信度阈值
```python
# 如果最高分 <= 0.35，归为 neutral
if best_score <= 0.35:
    return 'neutral', 0.3
```

---

## 7. 说话人匹配（SpeakerMatcher）

### 7.1 六级漏斗匹配

```
L1: 精确名称匹配      → confidence 0.95
L2: 语义相似度匹配   → BGE 向量，confidence 0.7~0.9
L3: 别名/头衔匹配     → confidence 0.85
L4: 触发词推理       → 动作/称呼触发词，confidence 0.6~0.8
L5: 活跃度推理       → 最后出现章节 + 对话频率，confidence 0.5~0.7
L6: 代词消解          → 最近活跃同性别角色，confidence 0.4~0.6
```

### 7.2 Phase 2 关键改进（38% → 63%）

#### 7.2.1 "通则推理、拒绝猜测、标识未知"哲学

**问题**：系统在证据不足时硬猜测具体身份，产生垃圾输出。

**解决方案**：
- 自称词（老臣、末将、属下）在上下文中无匹配时，返回"未知_文臣"、"未知_武将"、"未知_下属"等类型标注
- 不再猜测具体人物身份

#### 7.2.2 HanLP 词性验证

**问题**：动作词如"轻声问"、"沉声道"被误识别为角色名。

**解决方案**：
- `_is_verb()` 方法：使用 HanLP 词性标注验证候选角色名
- `_is_valid_character_name()` 方法：过滤包含动词、介词、量词的垃圾输出
- 预过滤已知说话修饰词（沉声、冷冷、淡淡等 20+ 个词）

**效果**：完全消除"轻声问"、"沉声道"等动词型垃圾输出。

#### 7.2.3 盲目猜测拦截

**问题**：无充分证据时，系统仍注册临时角色并返回猜测结果。

**解决方案**：
- `_register_temporary_character()` 安全检查
- 仅在上下文中存在语言行为提示（说道、点头、出现等）时才注册临时角色
- `_temp_char_cache` 内存缓存，避免跨 Session 污染
- `_extract_context_speakers()`：无候选时返回"未知_角色"

#### 7.2.4 核心词+修饰语模式

**核心词**（55个）：
```python
ROLE_CORE_WORDS = [
    '将军', '丞相', '元帅', '统领', '校尉', '大臣', '尚书', '宰相', '太傅',
    '总管', '掌门', '舵主', '堂主', '族长', '团长', '队长',
    '骑士', '剑客', '法师', '护卫', '士兵', '斥候', '杀手', '刺客',
    '盗贼', '佣兵', '猎人', '冒险者', '剑修', '修士',
    '管家', '丫鬟', '侍女', '侍卫', '铁匠', '商人', '牧师', '主教',
    '青年', '少年', '少女', '老者', '老人', '男子', '女子', '男人', '女人',
    '道士', '和尚', '僧人', '长老', '弟子', '前辈', '药老',
]
```

**修饰语正则**（1-6字符）：
```python
MODIFIER_PATTERN = (
    r'(?:'
    r'[黑白红蓝紫金银铁铜青灰赤绛翠黛玄]|'
    r'黑甲|白衣|青衣|红衣|蓝衣|紫衣|金甲|银甲|铁甲|'
    r'[老小长少中青幼]|'
    r'年[老迈轻少幼]|'
    r'[男女]|'
    r'[圣魔暗光血龙鹰狼凤虎蛇鬼]|'
    r'[未知神秘恐怖危险]|'
    r'中[年]|'
    r'[^\s，。！？\n「」『』""]{1,6}'  # 1-6字符，支持长修饰语
    r')?'
)
```

#### 7.2.5 NER 截断修复

**问题**："圣骑士团团长"被截断为"士团团长"。

**根因**：修饰语模式只允许1-2字符，但"圣骑士团"是4字符。

**解决方案**：将修饰语长度上限从2扩展至6。

---

## 8. 未知角色回填机制

### 8.1 设计背景

短文本上下文推理是**即时判断**——只看当前对话前后各两句，判断"这句话是谁说的"。优势是精准（显式提示100%），劣势是视野窄（角色名不在上下文窗口内就标记为未知）。

长文本频率分析是**统计积累**——全文扫描后基于出现频率、共现关系、右邻字多样性来过滤噪声、提升置信度。优势是能学到"谁是重要角色"，劣势是它不回答"这句话是谁说的"。

两套工具原来是分裂的，回填机制将它们打通。

### 8.2 工作流程

```
短文本推理 → 输出候选说话人（含"未知_角色"）
     ↓
累积3000字 → 触发冷启动
     ↓
频率分析聚类 → CharacterManager注册全文高频角色
     ↓
回填机制 → 用全文角色库重新匹配"未知_角色"
     ↓
更新speaker字段 → 完成二次消歧
```

### 8.3 实现细节

**`speaker_matcher.py` - `backfill_unknown_speakers()` 方法**：
```python
def backfill_unknown_speakers(self, sentences: list) -> int:
    """冷启动后用全文角色库回填未知说话人。"""
    if not sentences:
        return 0
    
    # 获取全文已确认的角色库
    confirmed_chars = list(self.char_manager._confirmed_characters.values())
    if not confirmed_chars:
        return 0
    
    # 构建角色名→角色映射（包括别名）
    char_map = {}
    for char in confirmed_chars:
        char_map[char.name] = char
        for alias in char.aliases:
            char_map[alias] = char
    
    backfill_count = 0
    for sentence in sentences:
        if not sentence.speaker.startswith("未知"):
            continue
        
        text = sentence.text
        # 按角色名长度降序排列，优先匹配长名字
        sorted_chars = sorted(char_map.items(), key=lambda x: len(x[0]), reverse=True)
        for char_name, char in sorted_chars:
            if char_name in text:
                sentence.speaker = char_name
                sentence.speaker_id = char.id
                backfill_count += 1
                break
    
    return backfill_count
```

**`pipeline_runner.py` - 集成点**：
```python
def _trigger_cold_start(self):
    # ... 现有聚类逻辑 ...
    
    # 冷启动聚类完成后，回填未知说话人
    backfill_count = 0
    for result in self._result_cache.get("default", []):
        count = self.speaker_matcher.backfill_unknown_speakers(result.sentences)
        backfill_count += count
    
    logger.info(f"冷启动完成：回填 {backfill_count} 个未知说话人")
```

### 8.4 预期效果

回填机制在**长篇小说**（数万字以上）中价值最大——当角色名在前3000字内未出现、但在后续章节中成为高频角色时，系统可以回头关联前面的"未知_角色"。

本质上是在模拟人类的阅读过程——读到第三章时你已经知道"黑甲骑士"是个重要角色，回头再读到第一章时自然会把他和之前的"未知_角色"关联起来。

---

## 9. 情绪标注 3 类简化

### 9.1 设计背景

情绪标注 L2(6类) 准确率仅 32%，尝试通过简化分类粒度来提升准确率。

### 9.2 映射规则

| L1 粗分类 (3类) | L2 细分类 (6类) | 说明 |
|----------------|----------------|------|
| excited (激动) | joy, anger, surprise | 高唤醒情绪 |
| subdued (压抑) | sadness, fear | 低唤醒情绪 |
| neutral (中性) | neutral | 平静、无明显情绪 |

### 9.3 实现

**评估脚本中的映射函数**：
```python
L2_TO_L1_MAP = {
    'joy': 'excited',
    'anger': 'excited',
    'surprise': 'excited',
    'sadness': 'subdued',
    'fear': 'subdued',
    'neutral': 'neutral',
}

def map_emotion_l2_to_l1(emotion: str) -> str:
    """L2(6类) → L1(3类) 映射"""
    return L2_TO_L1_MAP.get(emotion, emotion)
```

### 9.4 测试结果

| 指标 | L2(6类) | L1(3类) | 变化 |
|------|---------|---------|------|
| 准确率 | 32.0% | **33.0%** | +1% |
| 综合准确率 | 21.0% | 21.0% | - |

**预期 vs 实际**：
- 预期：30% → 55-65%
- 实际：32% → **33%**（仅+1%）

### 9.5 根因分析

3类化未能提升准确率的原因：
- 规则系统对绝大多数 case（70+条）预测为`neutral`
- `neutral→neutral`的L1映射无法修正任何错误
- L1映射只有在L2预测在同一L1桶内时才有效（如joy↔anger都→excited）
- 但系统根本没预测到那些情绪，大部分case都落入`neutral`

**结论**：3类化本身不能解决"全预测neutral"问题。规则系统的瓶颈在于**情绪信号覆盖不足**，而非分类粒度。

---

## 10. NER 与实体管理

### 10.1 实体生命周期

```
NER 提取 (HanLP)
   → 基础过滤 (_filter_false_persons)
   → 上下文多样性验证 (ContextDiversityValidator)
   → 说话角色过滤 (SpeakerRoleFilter)
   → 实体链接 (EntityLinker)
   → 实体聚类 (EntityClusterer) → 写回 CharacterManager
   → 最终实体列表 (linked_entities)
```

### 10.2 实体数据结构

```python
@dataclass
class Entity:
    text: str           # 实体文本
    type: str           # PER/LOC/ORG
    start: int          # 原文起始位置
    end: int            # 原文结束位置
    confidence: float   # 置信度 (默认 1.0)
```

### 10.3 关键优化

1. **虚假人名过滤**：基于词典和规则过滤非人名 PER 实体
2. **外文名合并**：将 `名·姓` 格式合并为单个实体
3. **单字实体边界检查**：避免"树" 匹配到"树林"
4. **高频实体保留**：出现 >= 3 次的实体跳过对话上下文检查
5. **NER 截断修复**：修饰语长度上限从2扩展至6，解决"圣骑士团团长"等长复合词截断

---

## 11. 接口定义规范

### 11.1 PipelineRunner 主接口
```python
class PipelineRunner:
    def analyze_chapters(
        self,
        text: str,                          # 完整小说文本
        start: int = 0,                     # 起始章节索引
        end: int = -1,                      # 结束章节索引
        force: bool = False,                # 强制重新分析
        cache_key: str = "default",         # 缓存键
        progress_callback: Optional[Callable] = None,  # 进度回调
    ) -> List[ChapterResult]:
        """分析章节的完整流程"""

    def export_json(self, results: List[ChapterResult], pretty: bool = True) -> str:
        """导出为 JSON 格式"""

    def export_ssml(self, results: List[ChapterResult]) -> str:
        """导出为 SSML 格式"""

    def pause(self) -> None:
        """暂停流水线"""

    def resume(self) -> None:
        """恢复流水线"""

    def get_progress(self) -> ProgressInfo:
        """获取当前进度"""
```

### 11.2 模块单例接口

所有核心模块提供全局单例访问：
```python
get_nlp() -> NLPBasics
get_emotion_extractor() -> EmotionExtractor
get_emotion_tagger() -> EmotionTagger
get_character_manager() -> CharacterManager
get_semantic_ranker() -> SemanticRanker
get_entity_clusterer() -> EntityClusterer
get_entity_linker(char_manager) -> EntityLinker
get_context_validator() -> ContextDiversityValidator
get_speaker_role_filter() -> SpeakerRoleFilter
get_pipeline_runner() -> PipelineRunner
```

### 11.3 输出数据格式（JSON）
```json
[
  {
    "chapter_id": 0,
    "title": "第一章 重生",
    "volume": 1,
    "statistics": {
      "total_sentences": 120,
      "dialogue_count": 45,
      "narration_count": 75,
      "sfx_count": 3,
      "entity_count": 8
    },
    "sentences": [
      {
        "text": "你给我滚出去！",
        "type": "dialogue",
        "speaker": "林雪",
        "emotion": "anger",
        "speed": 1.0,
        "tone": "normal",
        "sfx": [],
        "entities": [],
        "sentence_id": 1,
        "quotation_type": "dialogue"
      }
    ]
  }
]
```

---

## 12. 性能指标

### 12.1 算法指标（100条测试集）

| 模块 | 指标 | 历史 | 修仙 | 西幻 | 都市 | 总体 |
|------|------|------|------|------|------|------|
| 角色识别 | 准确率 | 70.6% | 56.5% | 50.0% | 70.0% | **63.0%** |
| 情绪标注 L2 | 准确率 | 41.2% | 21.7% | 30.0% | 30.0% | **32.0%** |
| 情绪标注 L1 | 准确率 | - | - | - | - | **33.0%** |
| 综合准确率 | - | - | - | - | - | **19.0%** |

### 12.2 完整小说回归测试

| 测试文件 | 对话召回 | 说话人准确率 | 综合得分 |
|---------|---------|------------|---------|
| test_novel_urban | 100.0% | 76.2% | 88.1% |
| test_novel_western | 100.0% | 64.5% | 82.3% |
| test_novel | 100.0% | 72.5% | 86.3% |
| **平均** | **100.0%** | **71.1%** | **85.5%** |

### 12.3 LLM 情绪基准测试

| 系统 | L2(6类) | L1(3类) |
|------|---------|---------|
| 规则系统 | 32.0% | 33.0% |
| Qwen2.5-1.5B | 27.0% | 26.0% |

**结论**：LLM 在单句情绪分类上表现不如规则系统，证明"情绪标注本身就是极难任务"。

### 12.4 资源限制

| 指标 | 限制 |
|------|--------|
| 最大输入文本 | 10 MB (10,485,760 字符) |
| 冷启动阈值 | 3,000 字符 |
| 内存限制 | 3.0 GB |

### 12.5 处理速度（估算）

| 规模 | 预估时间 |
|------|---------|
| 10 章 (~3 万字) | ~5 秒 |
| 100 章 (~30 万字) | ~30 秒 |
| 500 章 (~150 万字) | ~2 分钟 |

*(基于 CPU 环境估算，实际速度取决于 HanLP 模型加载方式)*

---

## 13. 测试覆盖

### 13.1 测试模块列表

| 测试文件 | 测试数量 | 状态 |
|---------|---------|------|
| `test_context_diversity_validator.py` | 13 | 全部通过 |
| `test_speaker_role_filter.py` | 10 | 全部通过 |
| `test_entity_clusterer.py` | 14 | 全部通过 |
| `test_emotion_extractor.py` | 21 | 全部通过 |
| `test_backfill.py` | 3 | 全部通过（新增） |
| 其他测试 | - | 见原 v1.0 文档 |

### 13.2 评估脚本

| 脚本 | 用途 |
|------|------|
| `evaluate_full_pipeline.py` | 完整 Pipeline 评估（支持 L1/L2 双重对比） |
| `llm_emotion_benchmark_v2.py` | LLM 情绪标注 Benchmark V2（新增） |
| `test_backfill.py` | 回填机制测试（新增） |
| `run_comprehensive_test.py` | 完整小说回归测试 |

### 13.3 测试数据集

| 文件 | 类型 | 数量 |
|------|------|------|
| `role_emotion_gt_50.json` | 50条 GT 数据 | 历史10 + 修仙14 + 西幻9 + 都市17 |
| `role_emotion_gt_100.json` | 100条 GT 数据（新增） | 历史17 + 修仙23 + 西幻20 + 都市40 |
| `test_novel_urban.txt` | 都市小说 | 自定义 |
| `test_novel_western.txt` | 西幻小说 | 自定义 |
| `test_novel.txt` | 修仙小说 | 自定义 |

---

## 14. 当前问题与挑战

### 14.1 已知问题

| 编号 | 问题 | 严重程度 | 状态 |
|------|------|---------|------|
| P1 | **情绪标注准确率仅33%**：规则系统对绝大多数case预测为neutral，L1/L2映射无法修正 | **高** | 维护模式 |
| P2 | **LLM情绪基准仅26%**：Qwen2.5-1.5B在单句情绪分类上不如规则系统 | **高** | 已验证 |
| P3 | **回填机制在短文本中价值有限**：角色名频繁出现，短文本推理已能处理 | 中 | 已接受 |
| P4 | **NER在都市文本泛化不足**：HanLP模型在网文场景准确率波动 | 中 | 待解决 |
| P5 | **情绪标注过拟合风险**：EmotionExtractor在35条GT上F1=1.000，需200+条验证 | 高 | 已部分验证（100条测试集情绪32%） |

### 14.2 技术挑战

#### 14.2.1 情绪标注是极难任务

**当前状态**：
- 规则系统 L1 准确率 33%
- LLM (Qwen2.5-1.5B) L1 准确率 26%
- 3类简化未能提升准确率

**结论**：情绪标注需要更长上下文、更大模型、或 fine-tune。规则系统和1.5B LLM 都只能做到30%左右。

#### 14.2.2 角色识别已达到规则系统天花板

**当前状态**：角色识别 63%（100条测试集）

**策略**：
- 冻结角色识别模块，不做新增功能
- 仅保留回归测试，发现 Bug 才修
- 回填机制已完成，在长篇小说中价值最大

### 14.3 架构改进方向

1. **流式处理**：当前按章节批处理，长文本（500+ 章）内存占用高
2. **并行化**：各章节处理相互独立，可并行
3. **缓存优化**：当前使用内存缓存，重启丢失
4. **Index-TTS 集成**：待执行（下一步最有价值的方向）

---

## 15. 依赖与环境

### 15.1 Python 依赖

| 包 | 版本 | 用途 |
|----|------|------|
| hanlp | 本地模型 | 中文 NLP |
| sentence-transformers | - | 实体聚类 |
| FlagEmbedding / BGE | - | 语义排序 |
| transformers | - | LLM 基准测试 |
| kokoro-onnx | - | TTS 引擎 |
| sqlite3 | 内置 | 数据存储 |

### 15.2 项目结构

```
novel-tts-engine/
├── pipeline/                    # 核心处理模块 (19 个文件)
│   ├── __init__.py
│   ├── chapter_splitter.py      # 章节划分
│   ├── dialogue_classifier.py   # 对话分类
│   ├── nlp_basics.py            # NLP 基础
│   ├── ner_extractor.py         # NER 提取
│   ├── context_diversity_validator.py  # 实体验证
│   ├── speaker_role_filter.py   # 角色过滤
│   ├── entity_linker.py         # 实体链接
│   ├── entity_clusterer.py      # 实体聚类
│   ├── character_manager.py     # 角色管理
│   ├── speaker_matcher.py       # 说话人匹配
│   ├── semantic_ranker.py       # 语义排序 (L2)
│   ├── sfx_detector.py          # 拟声词检测
│   ├── emotion_tagger.py        # 情绪标注 (旧规则)
│   ├── emotion_extractor.py     # 情绪提取 (新系统)
│   ├── quotation_classifier.py  # 引号分类
│   ├── pipeline_runner.py       # 流水线调度
│   ├── pipeline_context.py      # 上下文管理
│   ├── tts_generator.py         # TTS 生成
│   └── tts_kokoro.py            # Kokoro 引擎
├── tests/                       # 测试模块 (24+ 个文件)
├── scripts/                     # 评估脚本
├── analysis_reports/            # 分析报告 (40+ 份)
├── docs/                        # 开发文档
├── utils/                       # 工具模块
│   ├── config.py                # 配置
│   └── text_utils.py            # 文本工具
├── models/                      # 模型文件
│   └── hanlp/                   # HanLP 本地模型
└── novel_tts.db                 # SQLite 数据库
```

---

## 附录 A: 版本历史

| 版本 | 日期 | 内容 |
|------|------|------|
| v1.0 | 2026-05-04 | 首份分析模块完整技术文档 |
| **v2.0** | **2026-05-04** | **Phase 2 优化 + 回填机制 + 情绪3类 + LLM基准 + 100条测试集** |

### v2.0 详细变更

| 变更项 | 变更前 | 变更后 | 影响 |
|--------|--------|--------|------|
| 角色识别准确率 | 38% | **63%** | +25% |
| 情绪标注 L1 | N/A | **33%** | 新增 |
| LLM 情绪基准 | N/A | **26%** | 新增 |
| 测试集规模 | 50条 | **100条** | +100% |
| 回填机制 | 无 | **已实现** | 长篇小说价值大 |
| 修饰语长度 | 1-2字符 | **1-6字符** | 修复NER截断 |

---

## 附录 B: 相关文档索引

| 文档 | 位置 | 说明 |
|------|------|------|
| 开发手册 | `docs/开发手册_v4.0.md` | 完整开发手册（已合并分析模块技术文档） |
| 情绪标注问题分析 | `analysis_reports/算法报告/情绪标注根本性问题分析_20260503.md` | 双管道架构提议 |
| 角色识别阶段2测试报告 | `analysis_reports/优化报告/角色识别阶段2测试报告_20260504.md` | Phase 2 测试 |
| 角色识别模块优化总结报告 | `analysis_reports/优化报告/角色识别模块优化总结报告_20260504.md` | Phase 2 总结 |
| 角色与情绪识别100条测试报告 | `analysis_reports/优化报告/角色与情绪识别100条测试报告_20260504.md` | 100条测试 |
| 五步行动计划总结报告 | `analysis_reports/优化报告/五步行动计划总结报告_20260504.md` | 5步行动总结 |
| 项目全面体检 | `analysis_reports/测试报告/项目全面体检报告_20260503.md` | 项目整体评估 |

---

## 附录 C: 术语表

| 术语 | 说明 |
|------|------|
| NER | Named Entity Recognition，命名实体识别 |
| L1/L2 | 情绪分类层级：L1=3类粗分，L2=6类细分 |
| 双管道 | Pipeline 1 (角色) + Pipeline 2 (情绪) 并行 |
| 冷启动 | 前 N 章建立角色画像的过程 |
| F1 | Precision 和 Recall 的调和平均 |
| GT | Ground Truth，人工标注的标准答案 |
| Macro F1 | 所有类别 F1 的算术平均 |
| SSML | Speech Synthesis Markup Language |
| 回填 | 冷启动后用全文角色库回填未知说话人 |
| 核心词+修饰语 | 角色识别模式：核心词（55个）+ 修饰语正则 |

---

## 附录 D: 情绪标注验证数据（GT 标注 + 测试脚本 + 结果）

### D.1 GT 标注数据（35 条人工标注对话）

**数据来源**：三份测试文本中的人工标注对话（都市 15 条 + 西幻 10 条 + 斗破 10 条）。
**标注规则**：只标注有明显情绪信号的对话，平淡叙述归为 neutral。
**文件位置**：`scripts/evaluate_emotion_extractor.py`

（完整 GT 数据见 v1.0 文档附录 D）

### D.2 评估脚本（完整代码）

**文件**：`scripts/evaluate_emotion_extractor.py`

（完整代码见 v1.0 文档附录 D）

### D.3 测试结果

**运行时间**：2026-05-06

**EmotionExtractor vs 旧规则系统 F1 对比**：

| 情绪 | 旧规则 | EmotionExtractor | 提升 |
|------|--------|------------------|------|
| joy | 0.800 | **1.000** | +0.200 |
| anger | 0.667 | **1.000** | +0.333 |
| sadness | 0.889 | **1.000** | +0.111 |
| surprise | 0.667 | **1.000** | +0.333 |
| fear | 0.500 | **1.000** | +0.500 |
| neutral | 0.526 | **1.000** | +0.474 |
| **Macro F1** | **0.675** | **1.000** | **+0.325** |

### D.4 过拟合风险声明

**重要**：EmotionExtractor 在 35 条 GT 上 F1=1.000，但此结果**不代表系统已解决情绪标注问题**。

35 条全对在 ML 领域通常意味着测试集是量身定制的（overfit），而非模型真正有效。在至少 200 条以上、跨文体的独立测试集上验证真实 F1 之前，任何关于"情绪标注已解决"或"不需要 ML"的结论都是无效的。

**100条测试集验证结果**：
- 规则系统 L2 准确率：**32.0%**
- 规则系统 L1 准确率：**33.0%**
- LLM L1 准确率：**26.0%**

这证明情绪标注确实是极难任务，30% 左右的准确率反映了当前系统的能力边界。

---

## 附录 E: 五步行动计划总结

### E.1 行动清单

| 行动 | 状态 | 关键结果 |
|------|------|---------|
| **行动1：角色识别冻结** | ✅ 完成 | 基线63.0%（100条），已冻结 |
| **行动2：情绪3类简化** | ✅ 完成 | L1准确率33.0%（预期55-65%，实际仅+1%） |
| **行动3：LLM基准线** | ✅ 完成 | LLM L1准确率26.0%（低于规则系统33.0%） |
| **行动4：Index-TTS** | ⏳ 待开始 | 前置条件已就绪 |
| **行动5：情绪待机** | ✅ 完成 | 进入维护模式 |

### E.2 核心发现

**"情绪标注本身就是极难任务"的判断被数据验证**：
- 1.5B参数量的LLM在单句情绪分类上只能做到26%
- 规则系统反而比LLM高7个百分点（33% vs 26%）
- 问题不在于"角色缺失对情绪信号的压制"，而是**单句对话情绪分类本身就是需要更长上下文、更大模型的任务**

### E.3 系统当前状态

| 模块 | 准确率 | 策略 |
|------|--------|------|
| 角色识别 | **63.0%** | 冻结，仅Bug修复 |
| 情绪 L1(3类) | **33.0%** | 维护模式 |
| 情绪 L2(6类) | **32.0%** | 维护模式 |

### E.4 系统能力总结

**系统当前已能完成**：
- ✅ 多角色声线分离（63%准确率）
- ✅ 基础情绪标注（excited/subdued/neutral三分类）
- ✅ 诚实标注未知情况（不硬猜测）
- ✅ 未知角色回填（冷启动后二次消歧）

**系统当前的瓶颈**：
- ❌ 单句情绪识别（需要更长上下文）
- ❌ 含蓄/压抑情绪（sadness/fear等低唤醒情绪）
- ❌ 反讽/隐喻场景

### E.5 后续建议

**短期（已冻结）**：
- 角色识别：回归测试，仅Bug修复
- 情绪标注：维护模式，不做新开发

**中期（可选）**：
1. **Index-TTS集成**：最有价值的下一步，实际听感测试比任何数字都有说服力
2. **更大LLM测试**：用7B或14B模型测试情绪标注，看是否有显著提升
3. **多句上下文情绪推断**：利用对话历史（而非单句）做情绪标注

**长期**：
1. 情绪标注fine-tune（需要标注数据集）
2. 端到端TTS Pipeline优化
3. 测试集扩展至300+条

---

*文档结束*
