# Novel-TTS-Engine 开发手册

**版本**: v3.11
**最后更新**: 2026-05-03
**适用范围**: 小说文本 TTS 预处理引擎

---

## 目录

1. [项目概览](#1-项目概览)
2. [核心架构](#2-核心架构)
3. [模块开发规范](#3-模块开发规范)
4. [十大改进经验总结](#4-十大改进经验总结)
5. [已知问题与解决方案](#5-已知问题与解决方案)
   - [5.1 问题优先级与解决路径](#51-问题优先级与解决路径)
   - [5.2 详细问题清单](#52-详细问题清单)
   - [5.3 已知问题清单](#53-已知问题清单)
6. [测试与评估](#6-测试与评估)
7. [命名与文件规范](#7-命名与文件规范)

---

## 1. 项目概览

### 1.1 项目目标

将小说文本自动处理为 TTS 引擎可用的结构化数据，包括：
- 章节划分与对话识别（基础结构）
- 拟声词检测
- 说话角色识别（候选池质量）
- 对话-角色匹配（说话人分配）
- 端到端流程整合

### 1.2 技术栈

| 组件 | 技术 |
|------|------|
| NER | HanLP |
| 语义排序 | BGE-small-zh（512维，ONNX Runtime） |
| 章节划分 | 正则匹配（第X章/卷/回） |
| 对话分类 | DialogueClassifier（规则+统计） |
| 说话人匹配 | SpeakerMatcher（多漏斗匹配） |

### 1.3 当前基线（E.11 评估体系，新版6项指标）

| 文本 | 基础结构 | 拟声词 | 说话角色识别 | 对话-角色匹配 | 端到端 | 综合分 |
|------|---------|--------|------------|--------------|--------|--------|
| 都市异能 | 100.0 | 88.9 | 74.0 | 76.2 | 76.2 | 76.0 |
| 西幻 | 100.0 | 90.0 | 55.6 | 65.6 | 65.6 | 66.4 |
| 修仙 | 100.0 | 80.8 | 75.2 | 72.5 | 72.5 | 73.7 |
| **平均** | **100.0** | **86.6** | **68.3** | **71.4** | **71.4** | **72.0** |

**综合分计算公式**:
```
综合分 = 基础结构×0.10 + 拟声词×0.15 + 说话角色识别×0.30 + 对话-角色匹配×0.30 + 端到端正确率×0.10 - 稳定性罚分(最多5分)
```

**评估体系说明**:
| 指标 | 权重 | 说明 |
|------|------|------|
| 基础结构完整性 | 10% | 合并章节划分+对话分类，作为准入门槛 |
| 拟声词检测 | 15% | 保持独立，权重不变 |
| 说话角色识别 | 30% | 原NER重命名，评估候选池质量 |
| 对话-角色匹配 | 30% | 原说话人匹配，评估将对话分配给正确角色的能力 |
| 端到端正确率 | 10% | 对话被正确识别且匹配到正确说话人的比例 |
| 系统稳定性罚分 | 5% | 基于标准差，最多扣5分 |

---

## 2. 核心架构

### 2.1 数据流

```
输入文本 → chapter_splitter → dialogue_classifier → sfx_detector
  → nlp_basics(NER) → context_diversity_validator → speaker_role_filter
  → character_manager → speaker_matcher → semantic_ranker(L2)
```

### 2.2 关键设计模式

**全局单例模式**: 以下模块使用全局单例，避免重复加载模型：
- `get_nlp()` - NLPBasics
- `get_context_validator()` - ContextDiversityValidator
- `get_semantic_ranker()` - SemanticRanker
- `get_character_manager()` - CharacterManager
- `get_speaker_role_filter()` - SpeakerRoleFilter

**置信度管道**: 实体处理遵循四层过滤：
1. 原始 NER（HanLP 输出，confidence=0.9）
2. ContextDiversityValidator（统计验证 + 误合并检测）
   - 统计验证：根据右邻字多样性、出现次数、章节分布调整置信度
   - 误合并检测（2026-05-03 新增）：检测"人名+动词"误合并（如"萧炎冷"→"萧炎"+"冷"）
3. SpeakerRoleFilter（场景过滤，只保留说话角色）
   - L2 补偿限制（2026-05-03 修复）：不能超过原始置信度的 1.5 倍
4. EntityLinker（别名映射）

最终取 confidence >= 0.5 的实体。

**管道一致性保障**（2026-05-03 新增）:
- 统计验证降级的实体不能被后续组件提升到高于原始值的 1.5 倍
- 误合并检测在 `ContextDiversityValidator` 中执行，利用全文级别统计信息
- 已创建 `test_pipeline_consistency.py` 单元测试，防止未来违反此原则

### 2.3 目录结构

```
novel-tts-engine/
├── pipeline/          # 核心处理模块
│   ├── chapter_splitter.py
│   ├── dialogue_classifier.py
│   ├── sfx_detector.py
│   ├── nlp_basics.py
│   ├── context_diversity_validator.py
│   ├── speaker_role_filter.py
│   ├── character_manager.py
│   ├── speaker_matcher.py
│   ├── semantic_ranker.py
│   ├── emotion_tagger.py
│   └── pipeline_runner.py
├── tests/             # 测试文件和 GT 数据
├── analysis_reports/  # 测试报告和评估脚本
│   ├── 测试报告/
│   ├── 算法报告/
│   ├── 评估脚本/
│   ├── 调试脚本/
│   └── 测试原始数据/
├── models/            # ML 模型（BGE-small-zh）
├── db/                # 数据库
├── frontend/          # Streamlit 前端
├── scripts/           # 工具脚本
└── utils/             # 通用工具
```

---

## 3. 模块开发规范

### 3.1 NER 实体处理

**Entity 数据类**:
```python
@dataclass
class Entity:
    text: str
    type: str  # PER, LOC, ORG
    start: int
    end: int
    confidence: float = 1.0
```

**置信度阈值**:
- >= 0.8: 高置信度
- >= 0.5: 有效实体
- < 0.5: 过滤掉

### 3.2 说话人匹配规则

**匹配优先级**（从高到低）:
1. 显式说话人提示（"XX说道"）
2. 对话历史缓存（最近发言者）
3. 角色活动热度（最近活跃角色）
4. L2 语义相似度（BGE-small-zh）
5. 默认角色

**别名处理**: 必须在处理前注册角色别名，否则匹配失败。

### 3.3 对话上下文提取（SpeakerRoleFilter）

**支持四种模式**:
1. **引号前**: `"XX说道："` 左引号前 15 字符窗口
2. **引号后**: `"..."老陈说道。` 右引号后的"XX说道"
3. **引号中间**: `"..."，老陈说道，"..."` 对话中间的说话人提示
4. **声音描述**: `"..."是部门经理老陈的声音。` "是XX的声音/话"格式

**过滤策略**:
- 只对 PER 实体进行说话角色过滤
- ORG/LOC 实体直接放行
- 低频实体（< 2 次）降级置信度
- 多场景出现的实体通过 L2 提升置信度

---

## 4. 十大改进经验总结

### E.1-E.3: 基础管道建设
- **E.1**: 建立 v1 评估方法（简化正则）
- **E.2**: 使用完整算法管道替代测试脚本中的正则模拟
- **E.3**: 引入别名注册机制，提升说话人匹配率

### E.4-E.6: NER 精确率提升
- **E.4**: 按类型分别计算 NER F1（PER/LOC/ORG），更精准定位问题
- **E.5**: 增强 `_enhance_entities` 规则，支持"老X"、"X总"等中文称呼
- **E.6**: ContextDiversityValidator 上线，都市 NER 55.7% → 73.3%

### E.7-E.10: 说话角色收敛

- **E.7**: NER 规则增强对西幻产生负面影响，需要文体感知策略
- **E.8**: ContextDiversityValidator 白名单机制保护 GT 实体
- **E.9-E.10 说话角色收敛**: 从识别全文所有人名转为只识别对话中出现的说话角色。这个策略转变让 NER 和 SpeakerMatcher 的目标直接对齐——NER 的输出恰好是 SpeakerMatcher 需要的输入，不再有旁白角色污染候选池。SpeakerRoleFilter 上线后，E.10 扩展对话上下文提取到四种模式，都市 NER 稳定在 70%，西幻 NER 开始回升（53.0% → 55.6%）。这是本轮最重要的方向性决策。

### E.11-E.13: 管道一致性与误合并检测

- **E.11 管道断点发现**: 通过逐段调试发现 SpeakerRoleFilter 的 L2 补偿覆盖了 ContextDiversityValidator 的统计验证降级结果。"萧炎冷"被正确降级为 0.3，但 L2 补偿将其提升到 0.65，导致误报。
- **E.12 L2 补偿修复**: 限制 L2 补偿不能超过原始置信度的 1.5 倍，确保统计验证降级结果不会被意外覆盖。斗破 NER 从 85.7 提升到 92.3。
- **E.13 统计误合并检测**: 在 ContextDiversityValidator 中新增 `_detect_mis_merged_entities()` 方法，利用全文级别的统计信息检测"人名+动词"误合并（如"萧炎冷"→"萧炎"+"冷"）。移除 FALSE_PERSON_ENDINGS 黑名单，使用统计方法替代。

### 核心教训

| 经验 | 说明 |
|------|------|
| 测试脚本 ≠ 生产代码 | 必须用真实算法管道评估，不能用正则模拟 |
| 白名单保护 | GT 实体必须加入白名单，不受统计验证降级影响 |
| 上下文窗口要足够 | 15 字符窗口不够，需要支持引号后、引号中间等多种模式 |
| 类型过滤要精准 | 只对 PER 过滤，ORG/LOC 直接放行 |
| 文体差异要关注 | 都市和西幻的 NER 表现差异大，需要差异化策略 |
| 管道一致性 | 统计验证降级结果不能被后续组件覆盖，L2 补偿必须有上限 |
| 黑名单不可取 | 中文人名可包含任何字符，硬编码黑名单会导致误杀真实人名 |
| 统计验证优于硬编码 | 用全文统计信息（出现次数、右邻字多样性）替代人工词表 |

---

## 5. 已知问题与解决方案

### 5.1 问题优先级与解决路径

| 优先级 | 问题 | 影响范围 | 解决路径 |
|--------|------|---------|---------|
| **P0** | 西幻 说话角色识别 55.6% | 西幻文体泛化能力 | 角色聚类模块（蓝图第三阶段后半段），通过实体链接合并同一角色的多种称呼 |
| **P1** | 对话-角色匹配 76.2% | 都市异能体验 | L2 在 SpeakerMatcher 内部建立对话历史缓存（当前只在评估脚本中建立） |
| **P2** | 群体说话人（"三人"、"众人"） | 所有文体 | 规则层处理，"三人/众人"映射为特殊标记 |
| **P3** | 西幻 L2 触发不稳定 | 西幻语义排序 | 排查 ONNX 和 PyTorch 后端切换导致的模型加载路径问题 |

### 5.2 详细问题清单

**说话角色识别（原 NER）问题**:

| 问题 | 根因 | 状态 |
|------|------|------|
| "老陈"被 HanLP 识别不完整 | 简称识别困难 | ✅ 已解决（模式4） |
| "张总"被误认为说话角色 | 出现在对话上下文中但不是 GT 角色 | ⚠️ 部分解决 |
| 单字人名（"苏"、"张"）误检 | HanLP 将单字识别为 PER | ⚠️ 部分解决 |

**对话-角色匹配问题**:

| 问题 | 根因 | 状态 |
|------|------|------|
| 西幻说话人匹配偏低（65.6%） | 对话格式复杂，跨行对话多 | ❌ 待解决（P1） |
| L2 语义排序效果不明显 | BGE 模型对角色名相似度区分度不够 | ⚠️ 待优化 |

**拟声词检测问题**:

| 问题 | 根因 | 状态 |
|------|------|------|
| 部分拟声词不在词典中 | DEFAULT_SFX_WORDS 不完整 | ⚠️ 部分解决 |
| 重复拟声词只检测一次 | 去重逻辑过于激进 | ⚠️ 待优化 |

### 5.3 已知问题清单

**总计**: 17项（高优先级: 2项，中优先级: 11项，低优先级: 4项）

| 编号 | 问题描述 | 影响模块 | 相关经验 | 优先级 | 解决方案 |
|------|---------|---------|---------|--------|---------|
| E-01 | 西幻人名 NER F1 偏低（53%） | NER 基础识别 | E.7 | 中 | 建立文体感知规则，西幻场景下降低中文简称权重 |
| E-02 | "老陈"被 HanLP 识别不完整 | NER 增强规则 | E.5 | 中 | 增加"老X"、"小X"等模式匹配规则 |
| E-03 | ContextDiversityValidator 过度降级 GT 实体 | 统计验证 | E.8 | ✅ 已解决 | 引入 GT 白名单机制，保护已知角色 |
| E-04 | 引号前15字符窗口不够覆盖说话人提示 | 上下文提取 | E.9 | ✅ 已解决 | 扩展到引号后、引号中间、声音描述等四种模式 |
| E-05 | ORG/LOC 实体被错误过滤 | SpeakerRoleFilter | E.10 | ✅ 已解决 | 类型过滤只对 PER 生效，ORG/LOC 直接放行 |
| E-06 | 对话-角色匹配率低（西幻 65.6%） | SpeakerMatcher | - | 高 | 建立对话历史缓存，增加跨行对话处理 |
| E-07 | 群体说话人（"三人"、"众人"）无法识别 | 说话角色识别 | - | 低 | 规则层处理，映射为特殊标记 |
| E-08 | BGE 模型对角色名相似度区分度不够 | L2 语义排序 | - | 中 | 微调排序权重，增加基于上下文的相似度计算 |
| E-09 | 别名未注册导致匹配失败 | SpeakerMatcher | E.3 | 中 | 增强别名自动发现机制 |
| E-10 | 低频实体（<2次）被过度降级 | ContextDiversityValidator | - | 低 | 引入重要性评分，区分一次性角色与噪声 |
| E-11 | 西幻 L2 触发不稳定 | 语义排序 | P3 | 中 | 排查 ONNX/PyTorch 后端切换问题 |
| E-12 | 章节划分对无标题章节漏检 | chapter_splitter | - | 低 | 增加语义边界检测作为补充 |
| E-13 | 拟声词词典不完整，部分拟声词漏检 | sfx_detector | - | 中 | 建立动态词典更新机制 |
| E-14 | 西方人名识别在文体感知开关开启时可能漏掉多个译名变体（如"亚瑟"和"阿瑟"） | 说话角色识别 | FABLE + GenWebNovel | 中 | 建立译名变体词典，合并同名异译 |
| E-15 | ContextDiversityValidator 未考虑实体间的共现关系，可能误杀低频但真实性高的实体（如一次性出场的信使） | 说话角色识别 | BookWorld | 中 | 引入共现统计作为辅助验证 |
| E-16 | SpeakerMatcher 缺少对"角色头衔"、"角色关系触发词"的利用（如"国王说"、"骑士回答"） | 对话-角色匹配 | RKGC+CBA | 高 | 建立头衔-角色映射，增强触发词匹配 |
| E-17 | SpeakerRoleFilter 的固定窗口提取可能捕获不到长距离的"是XX的声音"模式 | 说话角色识别 | NUCS | 中 | 实现动态上下文选择（NUCS） |
| E-18 | SpeakerRoleFilter 的 L2 补偿过度提升，覆盖统计验证降级结果 | 管道一致性 | - | ✅ 已解决 | 限制 L2 补偿不超过原始置信度的 1.5 倍 |
| E-19 | HanLP 将"人名+动词"误合并为更长人名（如"萧炎冷"） | 误合并检测 | - | ✅ 已解决 | 统计误合并检测（_detect_mis_merged_entities） |
| E-20 | FALSE_PERSON_ENDINGS 黑名单无法处理真实人名 | 误杀风险 | - | ✅ 已解决 | 使用统计方法替代黑名单 |

---

## 6. 测试与评估

### 6.1 运行测试

```bash
# 运行 v3 泛化性测试
python analysis_reports/评估脚本/run_v3_tests.py

# 指定小说和 GT 文件
python analysis_reports/评估脚本/evaluate_generalization_v3.py \
  tests/test_novel_urban.txt \
  tests/test_novel_urban_ground_truth.json
```

### 6.2 GT 文件格式

```json
{
  "novel": "test_novel_urban",
  "style": "都市异能",
  "chapters": {
    "total_expected": 7,
    "volumes": 2,
    "chapter_titles": ["第一章 异能觉醒", ...]
  },
  "对话分类": {
    "总对话数": 42
  },
  "sfx": {
    "sfx_words": ["轰隆", "劈啪", ...]
  },
  "entities": {
    "persons": ["苏夜", "林雪", ...],
    "speaking_persons": ["苏夜", "林雪", ...],
    "organizations": ["暗影会", ...],
    "locations": [],
    "aliases": {
      "苏夜": ["苏先生"]
    }
  },
  "dialogue_speakers": [
    {"text": "苏夜，你在这里！", "speaker": "林雪"}
  ]
}
```

**关键字段**:
- `speaking_persons`: 仅包含实际说话的角色（E.9 新增）
- `aliases`: 角色别名映射
- `dialogue_speakers`: 对话-说话人映射

### 6.3 评估标准

| 得分 | 状态 |
|------|------|
| >= 90 | 生产可用（优秀） |
| >= 85 | 生产可用（良好） |
| >= 75 | 基本可用 |
| < 75 | 需要改进 |

### 6.4 如何添加新测试文本

添加新文体测试的步骤：

**步骤1：准备测试小说**
- 编写或收集目标文体的小说文本，保存为 `tests/test_novel_<类型>.txt`
- 建议长度：3000-10000 字，覆盖至少 5 个章节

**步骤2：编写 GT 文件**
- 创建 `tests/test_novel_<类型>_ground_truth.json`
- 按 v3 格式编写，重点标注以下字段：
  - `speaking_persons`：仅包含实际说话的角色（不含旁白中出现的角色）
  - `dialogue_speakers`：逐条标注每句对话的说话人
  - `persons`：所有出现的人名（包含不说话的配角）
  - `organizations`、`locations`：组织和地点
  - `aliases`：角色别名映射

**步骤3：运行评估**
```bash
python analysis_reports/评估脚本/evaluate_generalization_v3.py \
  tests/test_novel_<类型>.txt \
  tests/test_novel_<类型>_ground_truth.json
```

**步骤4：记录结果**
- 将各项得分记录到 `analysis_reports/测试报告/泛化性测试报告_20260502_001700.md` 中
- 对比已有文体的得分，分析差异原因
- 如果某个模块得分低于 75，需要单独编写调试脚本定位问题

**GT 文件编写注意事项**:
- `speaking_persons` 是 NER 评估的关键，必须准确标注所有实际说话的角色
- `dialogue_speakers` 中的 `text` 字段只需截取对话的前 30 字即可
- 如果存在交织对话（同一行多人说话），需要拆分为多条记录

---

## 7. 命名与文件规范

### 7.1 文件命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 测试报告 | `中文类型_YYYYMMDD_HHMMSS.md` | `泛化性测试报告_20260502_001700.md` |
| 算法方案 | `中文描述_YYYYMMDD.txt` | `说话角色过滤器SpeakerRoleFilter完整方案_20260502.txt` |
| 测试小说 | `test_novel_类型.txt` | `test_novel_urban.txt` |
| GT 文件 | `test_novel_类型_ground_truth.json` | `test_novel_urban_ground_truth.json` |

### 7.2 目录分类

```
analysis_reports/
├── 测试报告/        # .md 测试报告
├── 算法报告/        # .txt 算法方案
├── 评估脚本/        # 评估运行脚本
├── 调试脚本/        # 临时调试脚本
└── 测试原始数据/    # 测试用原始文本文件
```

### 7.3 根目录规则

- **根目录必须为空**: 禁止在 `analysis_reports/` 根目录放置 .md/.txt 文件
- **禁止嵌套**: 不允许出现子目录中的子目录
- **定期清理**: 调试脚本用完后移入 `调试脚本/` 或删除

---

## 附录：版本历史

| 版本 | 日期 | 主要变更 |
|------|------|---------|
| v3.0 | 2026-05-02 | v3 评估方法上线 |
| v3.6 | 2026-05-02 | ContextDiversityValidator 上线 |
| v3.9 | 2026-05-02 | SpeakerRoleFilter 上线 |
| v3.10 | 2026-05-02 | 对话上下文扩展（四种模式） |
| v3.11 | 2026-05-03 | 管道一致性修复：L2 补偿限制 + 统计误合并检测，斗破 NER 85.7 → 92.3 |
