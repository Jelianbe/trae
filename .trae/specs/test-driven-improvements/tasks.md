# 基于全面功能测试的改进 — 任务清单

## 任务清单

### T-NEW-1: 更新改进方案文档 ✅
将测试分析中识别的修复建议整合到 `docs/代码审查与改进方案_v6.1_20260513.md` 中。

- [x] 读取当前改进方案文档全文
- [x] 新增 Section 5.5 "测试驱动的修复方案"（按用户标准重新分类）
  - [x] P0-NEW-A: 评估上下文修复（184条"未知"中的大部分，评估基础设施）
  - [x] P0-NEW-B: 对话边界检测（29条FP，返回具体人的非对话）
  - [x] P1-NEW: 说话人匹配逻辑优化（38条选错人）
  - [x] P1-NEW-1: entity_cleaner 候选保留策略调优
- [x] 更新 DoD 目标值（P0基线30.4%，P1目标~65%，P2目标≥75%）
- [x] 更新严重性总览，加入测试发现的新问题
- [x] 更新架构依赖图（P0-NEW-A/B 引用）
- [x] 通过 R-007 双向绑定检查

### T-NEW-2: 评估上下文修复（P0-NEW-A）
解决 GT 短用例缺上下文导致的184条"未知"（这是评估脚本问题，不是管道bug）。

- [ ] 修改 `merge_gt_tests.py`：提取短用例时保留完整段落上下文
  - [ ] 从原文中截取对话前后各1~2句作为 context
  - [ ] 或者：使用整段/整章作为输入
- [ ] 修改 `test_data_unified.py` 格式：每个用例包含 `full_paragraph`（用于实际评测）和 `dialogues`（用于验证）
- [ ] 修改 `evaluate_unified.py`：使用 `full_paragraph` 作为输入，逐条匹配 `dialogues` 期望
- [ ] 验证：基线准确率从 30.4% 提升至约 60%

### T-NEW-3: 对话边界检测（P0-NEW-B）
解决29条非对话被检测为对话且返回具体说话人的问题。

- [ ] 设计 `pipeline/dialogue_boundary_detector.py` 模块
  - [ ] 基于说话动词锚定（引用 speech_verb_detector.py）
  - [ ] 基于引号内容结构分析（句法分析，非whitelist）
  - [ ] 支持古籍/石碑 pattern 抑制
- [ ] 集成到 `speaker_matcher.py` 的对话检测前置步骤
- [ ] 编写单元测试覆盖29个FP场景
- [ ] 验证：non_dialogue 类 FP（返回具体人）从29降至≤5
- [ ] 验证：不引入新的 FN（不误杀真实对话）

### T-NEW-4: 说话人匹配逻辑优化（P1-NEW）
解决38条对话检测到但说话人选错的问题。

- [ ] 实现说话动词绑定：`"X怒道"` 明确绑定 X 为说话人
- [ ] 实现呼语排除：`"X，你好"` 中 X 是听话人（N-1 落地）
- [ ] 实现动作紧邻关联：`"X冷笑着"` 绑定 X 为说话人（N-2 落地）
- [ ] 编写单元测试覆盖38个错误场景
- [ ] 验证：选错人从38降至~10

### T-NEW-5: entity_cleaner 候选保留策略调优（P1-NEW-1）
- [ ] 分析当前 `_is_clean_per_entity` 的过滤行为
- [ ] 识别过度过滤的模式（保留了多少、误杀了多少）
- [ ] 调低过滤阈值或调整过滤优先级
- [ ] 编写单元测试覆盖边界场景
- [ ] 验证：不降低精确率的前提下，召回率提升 ≥ 5%

### T-NEW-6: 执行测试驱动改进后的回归验证
- [ ] 运行 `evaluate_unified.py` 模式B，确认准确率提升至 ≥ 65%
- [ ] 运行边界条件测试，确认无回归
- [ ] 运行异常处理测试，确认无回归
- [ ] 更新 `分析模块全面功能测试报告` 基线

## 任务依赖

- T-NEW-1 无依赖（文档更新）
- T-NEW-2 无依赖（评估脚本独立，优先执行）
- T-NEW-3 依赖 P0-1（speech_verb_detector 已完成）
- T-NEW-4 依赖 N-1/N-2（已有设计）
- T-NEW-5 依赖 P1-2（"的"字结构检测）
- T-NEW-6 依赖 T-NEW-2 + T-NEW-3 + T-NEW-4 + T-NEW-5

## 执行顺序

### 第一波：文档更新（立即） ✅
1. T-NEW-1: 更新改进方案文档

### 第二波：评估脚本修复（独立，最高优先级）
2. T-NEW-2: 评估上下文修复（获得真实基线）

### 第三波：对话边界检测（代码级修复）
3. T-NEW-3: 对话边界检测

### 第四波：说话人匹配优化 + entity_cleaner 调优（可并行）
4. T-NEW-4: 说话人匹配逻辑优化
5. T-NEW-5: entity_cleaner 候选保留

### 第五波：回归验证
6. T-NEW-6: 运行完整测试，确认效果
